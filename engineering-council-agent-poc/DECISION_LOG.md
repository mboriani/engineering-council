# DECISION LOG — Engineering Council Agent (POC)

Chronological log of decisions. Significant/architectural ones are promoted to
an ADR under `docs/adr/`. Newest first.

| # | Date | Decision | Rationale | Link |
|---|------|----------|-----------|------|
| D-111 | 2026-09-10 | **M16 Graph-Assisted Context — CLOSED / ACCEPTED**: graph-assisted context is accepted for production/practical Council usage. Graphify 0.9.53 provides deterministic local graph extraction without LLM dependency. Currently opt-in; Security + Architecture supported; minimal navigation context <= 3000 chars; persistent graph cache with cross-run reuse validated; graceful fallback when Graphify unavailable/fails/timeout; package schema remains 1.1; known untracked-source limitation documented; no further M16 experiments planned. | M16.1-M16.3B completed and accepted. | `MILESTONE-016` |
| D-110 | 2026-08-19 | **Dedup False-Merge Hardening (M15.4A, ADR-026 amended)** — corrective milestone, no evaluation: fixed the single concrete M15.4 defect (false dedup merge `CQL-c55887984d`). Reproduced from persisted data first: RAW-010 (bundle, "Duplicated magic string for named HttpClient construction", secondary OBS-014 carrying `WriteLog`@ContextLogger.cs:42 / `OnResultExecutionAsync`@RequestFilter.cs:96) absorbed RAW-014 ("...magic string keys for context items (CustomerID/BrandID casing)", OBS-117 `WriteLog`@:42 / `OnResultExecutionAsync`@:110) because titles shared the generic 2-token phrase {magic, string} and `MinFocusTitleTokens=2` could not tell a shared CATEGORY from a shared DEFECT. **Fix (deterministic, conservative): primary-focus anchor** — a shared dedup method-part must be a PRIMARY-focus method-part of BOTH findings: attached to an observation whose normalized-title similarity to the finding's own title ≥ `PrimaryFocusAnchorSimilarity = 0.30` (exact `TitleNormalizer.Similarity` Jaccard); close-lines scoped to primary-focus evidence; title-less obs never primary; bundles still merge on their primary issue (secondary obs lose identity power only). Threshold grounded in persisted M15.2/M15.4 similarity distributions — bimodal with a clean 0.12 gap: same-defect-rephrased obs ≥ 0.37 (OBS-081 0.37 / OBS-057 0.39 / OBS-070 0.38 / OBS-021 0.55 / OBS-032 0.58 / OBS-139 0.38), distinct-sub-issue obs ≤ 0.25 (RAW-010 vs OBS-014 = 0.25 max). Corpus document-frequency "distinctive token" approach TRIED and REJECTED ({magic,string} DF 2/55 = 0.04 — identical to genuinely distinctive tokens; no hardcoded blacklist). Score stays 0.9 (predicate fixed, not cosmetics); cluster-order safety intrinsic (anchor per finding from its own obs+title; equivalent permutations → equivalent partitions). **Real-data replay (persisted M15.4, NO providers): 55 → 50 consolidated** (was 49), dedup 6 → 5 joins, clusters 4 → 3 all genuine — REL-42ba184796 (RAW-017/018/020/025 wallet HttpClient, evid/prov/sev preserved), TST-d4244b718f (RAW-035/036 brand-auth), OBS-70131abcc2 (RAW-045/049 readiness); **RAW-010 separate from RAW-014: YES** (each standalone: RAW-010 OpenCode+Codex/10 obs, RAW-014 ClaudeCode/1 obs). M15.2 replay unchanged 29→27 (wallet pair merges via rephrased obs 0.39/0.38 — anchor is NOT exact-title). All 5 named near-match pairs (RAW-046/051, RAW-050/055, RAW-026/043, RAW-023/034, RAW-002/044) stay separate. Diagnostics reflect the ACTUAL partition (PreDedup 55 / PostDedup 50 / Deduplicated 5 — old 49 NOT forced); severity/confidence/CouncilAssessment/health scoring untouched; `schemaVersion` stays **1.1**, no new package/domain fields, consumer DTO unchanged. Build 0 warnings/0 errors; **618/618 tests** (+16 `DedupFalseMergeHardeningTests` incl. persisted false-merge regression, reversed + cluster-order permutations, generic-2-token insufficient, secondary-inheritance insufficient, title-less obs no identity, rephrased-primary still anchors, all 3 genuine M15.4 merges, evidence/provenance/severity/IDs/diagnostics/schemaVersion, persisted M15.4 replay 55→50 + near-match separation; existing dedup/replay/package fixtures made faithful — primary obs now carry the finding's title like the real pipeline). Docs: MILESTONE-015.4A, PROJECT_MEMORY v37, ADR-026 amended (prefer amend over new ADR), RUNBOOK unchanged. No providers invoked; no new Council run; next milestone NOT started. | The M15.4 verdict named the single next problem with evidence (a generic 2-token phrase + secondary-symbol inheritance letting a multi-issue bundle absorb a distinct finding). The primary-focus anchor is the minimal, conservative, deterministic fix: it reuses the exact same identity machinery (suffix-compatible method-part + close lines) but requires the shared symbol to be PRIMARY in both findings, which is exactly the discriminating characteristic the four M15.4 merges expose (3 genuine: shared method-part primary in both members; 1 false: shared only via the bundle's secondary obs). The 0.30 threshold sits in a measured 0.12 gap with >1× margin on both sides, and the M15.2 wallet merge proves the anchor handles same-defect rephrasing without requiring exact titles. Rejected more aggressive options deliberately: DF-based "distinctive tokens" (not corpus-generic), hardcoded blacklists (forbidden by the milestone), lowering/raising the score (cosmetic — the predicate was the defect), and global multi-issue bundle bans (would lose genuine M15.2/M15.4 bundles). Precision > dedup volume: 50 > 49 consolidated is the honest, correct outcome. | `MILESTONE-015.4A` |
| D-109 | 2026-08-19 | Second Real-Repository Evaluation of the M15.3 improvements (M15.4) — **evaluation only, no production change**: re-ran the full 3-provider Council (OpenCode+Codex+ClaudeCode, 7 disciplines, MaxConcurrency 3, MaxAttempts 2, runtime-only timeouts 300/180/300) on the SAME target/commit as M15.2 (`RedirectToService` @ master `4a7d279…`, clean, `SNAP-23d782a35393` = M15.3C reference, `repositoryChangedDuringRun=false`). Run `20260819-195410-1086f6`, 1310.6 s wall. Results: **21/21 logical steps success** (2 OpenCode timeouts recovered by retry, 0 exhausted, 0 repairs), 154 obs (OpenCode 70/Codex 37/ClaudeCode 47), 55 raw → 49 consolidated, C1/H3/M25/L16/I4, aggr3=8/aggr2=6/aggr1=35, CouncilAssessment 11/3/35/0, multi 14, contradictions 0. **M15.3A coverage gates proven:** ALL 7 disciplines `coveredWithFindings` 3/3 (Architecture & Testing: 0/3 NoEvidence → 3/3; the M15.2 "no issues without evidence" defect is gone). M15.3C provenance proven: 0 unreproducible findings, snapshot pinned & unchanged. High-severity validation (critical SEC-45b47238ee plaintext `Telemetry:EncryptConfig` key/IV appsettings.json:107-112 + Sonar token in docker-compose; highs REL-42ba184796 wallet HttpClient no policy, OBS-aff05279c8 PII key cross-discipline view, REL-bddda24168 RequestFilter.cs:53 null-forgiving NRE) — ALL CONFIRMED; 5 lower-severity sample confirmed/plausible, 0 disputed/FP. **Dedup (M15.3D) verdict: 3/4 merges genuine, 1 FALSE MERGE `CQL-c55887984d`** — bundle RAW-010 ("Duplicated magic string for named HttpClient construction", secondary OBS-014 sharing `WriteLog`/`OnResultExecutionAsync`) absorbed RAW-014 ("magic string keys for context items (CustomerID/BrandID casing)") because titles share the generic 2-token phrase {magic, string}; the `MinFocusTitleTokens=2` guard is not discriminating enough (M15.2's ContextLogger/broad-catch traps avoided only because those titles shared ZERO tokens). 6 near-match pairs all correctly separate (RAW-046/051 focus-pass-no-symbol, RAW-050/055 config-only same-line false negative, RAW-026/043 SAME secret double-reported cross-discipline, RAW-023/034 and RAW-002/044 cross-discipline by design). Token telemetry ClaudeCode-authoritative only: 92092 total (in 236/out 91856), cacheRead 5687260 / cacheCreation 355333, fresh 355569 / activity 6042829, cacheReuseRatio 0.9412, ~1959 tok/obs; per-discipline split not separable (records carry no per-step tokens — honest limitation); OpenCode/Codex null. Retry cost: ~10 min OpenCode wall to 2×300 s timeouts, both recovered; timed-out attempt tokens unknown. Artifacts verified: `schemaVersion` **1.1**, reconciliation counts consistent (55→49, dedup 6, multi 14 + single 35), no secrets, no orphan Council processes. **VERDICT: B.** Next problem (documented, NOT fixed): make the dedup focus test more discriminating (distinctive content token, both titles naming the shared method-part, and/or refuse merge when the absorbing member is a multi-issue bundle). Docs: MILESTONE-015.4, PROJECT_MEMORY v36; no ADR, no contract change (`schemaVersion` 1.1 verified correct), RUNBOOK unchanged. Test suite NOT re-run (evaluation); last verified 600/600 (M15.3D). | The evaluation had to be evidence-driven and honest: the M15.3A-D fixes demonstrably worked on a second full run (full coverage with automatic recovery, coverage gates, pinned provenance, conservative dedup with all near-misses separate), so the verdict is a B, not a C — but the ONE genuine false merge (a generic 2-token topic phrase letting a multi-issue bundle absorb a distinct finding) is precisely the failure class M15.3D was designed to prevent, so it is not an A. The single next problem is thereby named with evidence (the CQL-c55887984d cluster), and — per the evaluation-only instruction — documented, never implemented here. | `MILESTONE-015.4` |
| D-108 | 2026-08-19 | Deterministic Finding Deduplication (M15.3D, ADR-026): new `dedup-identity` reconciliation stage (score 0.9) in `RuleBasedFindingReconciler` between `exact-rule-location` (0.95) and `type-symbol` (0.85), defining a **FindingDeduplicationKey**: two same-discipline findings are the same defect only when ALL hold — (1) shared suffix-compatible method-part symbol (`MethodPart` = last '.'-segment: `OnActionExecutionAsync` ≡ `RequestFilter.OnActionExecutionAsync` even when exact symbol sets don't intersect; evidence collected per observation AND per finding from `SymbolReferences`×`FileReferences`), (2) close lines in the SAME normalized file for that symbol (per-symbol evidence, `LineTolerance=3`), (3) **focus consistency = PRIMARY-ONLY exact significant-title-token overlap ≥ `MinFocusTitleTokens`=2** (stopword-filtered, length≥3; a verbose summary naming shared symbols is NEVER a merge signal), (4) no material severity contradiction (`|sevA−sevB| ≥ 2` refuses — explicit Low↔Critical contradiction never deduplicated; High↔Medium spread recorded as a `SeverityRange`, not erased). Merges EXACTLY the 2 genuine cross-provider duplicates in persisted M15.2 (`20260813-230240-b3e36e`): 29→**27** — RAW-012+RAW-016 (RequestFilter NRE, High) and RAW-011+RAW-014 (wallet HttpClient, `Medium–High`). Near-misses stay separate: health pair RAW-028/RAW-029 (no shared symbol), magic-string trio RAW-001/003/005, ContextLogger RAW-017 (RAW-012 bundle carries a secondary `WriteLog` but its title focus is the RequestFilter NRE), broad-catch RAW-015 (shares `GetContextAsync`/`RedirectToAsync` with the NRE pair but title focus differs), cross-discipline RequestFilter views RAW-026 (observability)/RAW-004 (code-quality). `Signature` gains `MethodSymbols`/`SymbolEvidence`/`TitleTokens` (Ordinal, order-independent); `Cluster.DedupJoins`; `ReconciliationSummary` gains **DEFAULTED (non-required — pre-M15.3D persisted docs still deserialize)** `PreDedupFindingCount`/`PostDedupFindingCount`/`DeduplicatedFindingCount` (PreDedup = PostDedup + Deduplicated; each join removes exactly 1 finding); external package gains the same additive fields on `reconciliation`, **`schemaVersion` stays 1.1**, consumer DTO gate updated (`EngineeringReviewPackageContract`). Markdown renders `Deduplicated findings: N (pre-dedup X → post-dedup Y)` only when >0. **Design lesson:** a secondary focus guard ("both name the shared symbol/file in title∪summary") was TRIED and REMOVED — it would false-merge RAW-015 into the NRE cluster because RAW-016's verbose summary literally names `BalanceContextProvider.GetContextAsync`/`RedirectAppService.RedirectToAsync`, surviving only by cluster-ordering luck; primary-only exact-title-token matching is the sole focus signal. Build 0/0; **600/600 tests** (+34: `DedupReconciliationTests` 24 offline — positive stage, suffix-compat, deterministic id, idempotence/order-independence, diagnostics counts, evidence preservation, negative fixtures A–H, bundle/focus guards, M15.2 regression pairs incl. `Medium–High` non-contradiction; `PersistedRunDedupReplayTests` 7 replaying the real persisted run — 29→27, exactly two `dedup-identity` merges, all near-misses standalone, reversed-input determinism; `PackageContractTests` 3 — additive schema, Markdown dedup line). No provider invocation; rule-based only. Existing 566 tests green (stage-3-type fixtures that earlier-stage rules correctly merge were redesigned to genuinely isolate the dedup stage). M15.3E not started. | M15.2 exposed two genuine cross-provider duplicates the conservative reconciler keeps separate; the milestone adds the smallest deterministic, symbol-grounded identity that merges exactly those and nothing else — false negatives remain preferred (ADR-011), the focus guard is exact-token-only so a verbose summary can never create a merge, severity spreads are recorded not erased, contradictions never deduplicated, and the contract grows additively (defaulted fields, schemaVersion 1.1, old persisted documents still deserialize). | `MILESTONE-015.3D` |
| D-107 | 2026-08-19 | Repository Snapshot Reproducibility (M15.3C): every `AnalysisRun` carries a deterministic description of the analyzed source state so "what exact source state produced this finding?" is answerable — fixing the M15.2 defect where finding CQL-7fe95e8d97 was unreproducible because the target changed after the run. New provider-neutral `RepositorySnapshotIdentity` (`Core.Domain`): `VersionControl` (`"git"` when positively identified — git work tree, or a `.git` dir when the executable is unavailable; null otherwise), `CommitSha` (full HEAD SHA; null for non-Git/unavailable/unborn), `Branch` (null for detached HEAD/non-Git), `IsDirty`/`HasUntrackedFiles` (bool? — tracked working-tree/index vs HEAD; `??` porcelain entries), `SnapshotFingerprint`, `RepositoryChangedDuringRun`. `SnapshotFingerprint.Compute` (`Core.Analysis`) = SHA-256 aggregate over the SAME post-ignore-rule scanned file selection (relative path + SHA-256 of raw file bytes per file, Ordinal order → `SNAP-<12 hex>`); relative-paths-only (machine-independent), no timestamps/randomness, no source contents (hashes only), unreadable file = stable marker. bin/obj/.git/node_modules/packages/artifacts/.vs excluded by the EXISTING `ScanOptions.IgnoredDirectories` — **no second independent definition of "repository files"**. `ContextFingerprint` is NOT repurposed (it identifies the provider effective context per its documented semantic). Additive: `AnalysisRun.RepositoryIdentity` (`repositoryIdentity` in run.json), package `repositorySnapshot` (CommitSha/Branch/IsDirty/HasUntrackedFiles/SnapshotFingerprint/RepositoryChangedDuringRun; **NO absolute local paths**; `schemaVersion` stays **1.1**), Markdown "Repository State" block (commit prefix; JSON keeps full values); consumer DTO gate updated (`RepositorySnapshotContract`). Read-only Git probe `GitSnapshotMetadataProvider` (`ProcessStartInfo.ArgumentList` argv, `UseShellExecute=false`, no shell strings) using ONLY `rev-parse --is-inside-work-tree`, `rev-parse HEAD`, `symbolic-ref --short HEAD`, `status --porcelain`; NEVER checkout/reset/stash/clean/add/commit; git unavailable or non-Git ⇒ metadata null and the run continues (`SnapshotFingerprint` still works). Capture timing: START captured in `AnalysisPipeline` BEFORE acquisition (step 1b); END verified ONCE after acquisition via a `LoadContent=false` re-scan (same selection, no content bodies); `RepositoryChangedDuringRun = start != end` — a detected mutation NEVER fails the run and NEVER replaces the start identity (the review describes the start state; the run warns). No file watcher, no continuous monitoring, and NO transactional-snapshot claim (start/end equality detects many mutations but not per-provider byte-equality under mid-mutation reads — documented limitation; immutable copies deferred to a future milestone). Git remote URLs never read/persisted (may contain credentials; out of scope). Build 0/0; **566/566 tests** (+25 offline/git: determinism/ordering/absolute-path invariance, content/add/delete, exclusions, dirty+untracked same-HEAD fingerprint change, clean/dirty/untracked/detached-HEAD/non-Git/git-unavailable, start=end/start≠end + retained start fingerprint, LoadContent selection equality, package serialization + privacy, Markdown provenance). Live validation (offline Mock, RedirectToService read-only): `git / 4a7d279aaa99898ea76128af12575b70431c401a / master / clean / SNAP-23d782a35393` — unchanged since M15.3B; dirty/untracked fixture proves same-HEAD fingerprint changes (`SNAP-2fa72dca3011`→`SNAP-da40a9a59af4`→`SNAP-4b4acd4b18ca`). No ADR (identity metadata; no architectural boundary change). | The Council run and any later verification must describe the SAME analyzed state; HEAD alone is not identity when the tree is dirty/untracked, so the fingerprint is derived from the exact file-selection+content facts the Council actually analyzes, captured before acquisition and verified once at the end — warning, never failing, never overwriting the start identity, and never exposing absolute paths or secrets. | `MILESTONE-015.3C` |
| D-106 | 2026-08-18 | Bounded acquisition-level retry (M15.3B): a logical acquisition step gets at most **ONE retry** — `Evidence:Execution:MaxAttempts`, default **2**, clamped [1,5], bound in `EvidenceOptions`/`CouncilOptions`(DI)/CLI `CliArgs`/API `EvidenceConfig` + BOTH shipped appsettings.json. **Retry-eligible: Timeout ONLY** — executor-detected (the step's linked timeout CTS fired) OR provider-reported (`LlmProviderException(Timeout)`, e.g. an agentic process exceeding its own bound). **Never retried:** run/user cancellation (propagates as `OperationCanceledException`, never a Timeout/Failure/RetryExhausted), `Authentication`/`Configuration`/`SchemaValidation`/`ProcessError` and any unexpected error (deterministic — retrying cannot help), success. The retry reuses the SAME effective provider timeout (M15.2B — no doubling, no adaptive growth), belongs to its logical step (same provider/request, held under the step's `MaxConcurrency` permit — never a second parallel slot), and never runs a third attempt. Reuses (does NOT duplicate) the existing provider-internal transient retry loop in the Claude/OpenAI chat clients (`LlmProviderException.IsTransient`: RateLimit/Timeout/Network/ServerError; `MaxRetries`) — unchanged. Telemetry: additive `AttemptCount` (default 1) + `RetryExhausted` (true ONLY when the final allowed attempt failed with a retry-eligible Timeout failure — "wanted to retry, had none left"; never for success or policy-prohibited failures) on `Evidence` + `ProviderExecutionRecord`; `RetryCount` now = bounded step-level retries + the FINAL attempt's provider-internal retries (0 = first attempt succeeded; the failed attempt's internal count is not double-counted — final attempt telemetry authoritative). Token telemetry never fabricated (unknown stays null, never 0). M15.3A coverage stays authoritative (no special-casing): retried success = successful evidence; exhausted = NoEvidence. Real validation `20260818-223806-aa8b1e` (RedirectToService, Architecture+Testing, OpenCode 300s/Codex 180s/ClaudeCode 300s, MaxConcurrency 3, MaxAttempts 2): **6/6 success, 0 failures, 0 timeouts, 0 retries needed** — every record carries `attemptCount:1/retryCount:0/retryExhausted:false` (policy live); Architecture and Testing both `coveredWithFindings` **3/3** (vs NoEvidence 0/3 in M15.2 `20260813-230240-b3e36e`); 8 findings (C0/H2/M4/L1); TotalTokens 28836 (2 known ClaudeCode executions), cache read 1,871,211 / creation 127,643 / ratio 0.935; acquisition ≈ 6m23s; cost/time LogicalSteps 6 · PhysicalAttempts 6 · RetriedSteps 0 · SuccessfulRetries 0 · ExhaustedRetries 0; package `schemaVersion` **1.1**, `providerExecution.records` carry the new additive fields, no per-record timeout leak, no orphan processes. Build 0/0; **541/541 tests** (+17 `BoundedRetryTests.cs`, offline: first-attempt telemetry, executor + provider-reported timeout → retry → success, timeout+timeout → RetryExhausted, never a third attempt, MaxAttempts=1 disables, SAME effective timeout reused, auth/config/schema/unknown never retried, cancellation never retried, retry stays inside its step's concurrency permit, internal+step retry counting, coverage interaction, config defaults + shipped keys). Known gaps documented: step-level retry Timeout-only by design (RateLimit/Network/ServerError stay chat-client-internal); per-attempt sub-durations not recorded (Duration covers the whole logical step). No ADR (executor-internal bounded policy, no architectural boundary change). M15.3C not started. | M15.2 proved timeout-recovery was the real operational gap (0/3 all Timeout) and M15.2B fixed timeout precedence; M15.3B closes the "one healthy run that still brushes its own timeout" case with the smallest honest lever — a SINGLE retry, Timeout-only, same timeout, inside the step's own concurrency permit — without exponential/adaptive/background retry, without a second retry subsystem (the chat clients' transient loop already exists and is reused), and without fabricating any telemetry or evidence. Deterministic failures must never be retried (retrying cannot change them); cancellation is user intent and must propagate untouched; only Timeout is a credible "slow-but-healthy" signal at the acquisition boundary. RetryExhausted is intentionally narrow (retry-eligible failure on the final attempt) so "policy forbade retry" (auth/schema/unknown) is never reported as "retries exhausted". | `MILESTONE-015.3B` |
| D-105 | 2026-08-18 | Evidence Coverage Gates (M15.3A): introduce a durable semantic distinguishing **CoveredWithFindings** (≥1 successful evidence acquisition AND consolidated findings) / **CoveredNoFindings** (≥1 successful acquisition, zero findings) / **NoEvidence** (zero successful acquisitions — timeout/failure NEVER counts as evidence) per REQUESTED discipline, plus SuccessfulProviders/AttemptedProviders counts (never a score/percentage; partial coverage like Security 2/3 is still valid evidence; no 3/3 requirement). Root cause: the first real M15.2 run (`20260813-230240-b3e36e`) reported Architecture 0/3 and Testing 0/3 as strengths ("No Architecture issues identified." / "No Testing issues identified.") — absence of evidence must never be presented as assurance. One authoritative calculation `DisciplineCoverage.From(ProviderExecutionReport, consolidatedFindings, requestedDisciplines)` computed by `EngineeringReviewPackageBuilder.Build` (order-independent, enum-ordered) — exporters/consumers never recompute. Prose fixes: `BuildStrengths` only emits "No {d} issues identified." for covered (non-NoEvidence) disciplines; `BuildExecutiveSummary` appends a coverage-limitation sentence; Markdown `CouncilFindings` renders every requested discipline with distinct wording ("No findings were identified from the available evidence." for CoveredNoFindings vs "No successful evidence was acquired for this discipline; absence of findings must not be interpreted as assurance." for NoEvidence, each with "Evidence coverage: N/M providers"; partial CoveredWithFindings sections show the count); `HealthAndRisk` adds a compact coverage-limitation note. Scoring (`HealthRiskScorer`) intentionally UNCHANGED — the report states the limitation rather than redesigning scores. ADDITIVE optional root package field `disciplineCoverage` (consumer `PackageContract` gate green; **schemaVersion stays 1.1** per the M014.3 additive precedent). Finding generation/reconciliation untouched. Build 0/0; **524/524 tests** (+20 `DisciplineCoverageTests.cs`, offline, incl. exact M15.2 regression shape + best-effort check against the persisted M15.2 artifacts). Remaining limitation: static-analysis-only findings with no provider-execution record read NoEvidence 0/0 by design; health scores exclude missing evidence (stated in the report). | Zero successful acquisitions is observationally different from "evidence shows no findings", and the real M15.2 report conflated them. A durable, deterministic, provider-neutral coverage semantic is required so no downstream renderer or consumer can ever present lack of evidence as assurance — and so future robustness milestones (M15.3B) can measure exactly which disciplines are uncovered. | `MILESTONE-015.3A-evidence-coverage-gates.md` |
| D-104 | 2026-08-18 | Token Efficiency Analysis (M15.2D): introduce THREE provider-neutral DERIVED metrics over the existing M12.1/M15.2C authoritative telemetry — all SumKnown, unknown stays null never 0, all "ACTIVITY" accounting (never cost/billable/unique-context/window occupancy). `ContextTokenActivity?` = SumKnown(InputTokens, CacheCreationInputTokens, CacheReadInputTokens) = observed input-side context-token activity; `FreshContextTokens?` = SumKnown(InputTokens, CacheCreationInputTokens) = input-side activity not reported as cache reads; `CacheReuseRatio?` = CacheReadInputTokens / ContextTokenActivity (only when denominator meaningful > 0; null otherwise). Computed centrally by `TokenEfficiencyMetrics` (Core.Domain, provider-neutral — any future provider with equivalent cache telemetry feeds the same calcs), stored per execution on `ProviderExecutionRecord` (set by `EvidenceAcquisitionExecutor.Record` from authoritative raw fields, single computation path), and aggregated at run level on `ProviderExecutionReport`: `TotalContextTokenActivity?`/`TotalFreshContextTokens?` (SumKnown) and report-level `CacheReuseRatio` = `TotalCacheReadInputTokens / TotalContextTokenActivity` — computed from AGGREGATE authoritative totals, NEVER an average of per-execution ratios. `TotalTokens = SumKnown(Input, Output)` and `KnownTokenExecutionCount` semantics UNCHANGED (cache/derived never folded in). External `engineering-review-package.json` unchanged: `EngineeringReviewPackageBuilder.PackageExecutionView` strips BOTH the M15.2C cache fields and the M15.2D derived fields → `schemaVersion` **1.1**, no derived/cache leak; derived metrics live in `provider-execution.json` (+ `run.json`) only. NO provider invocation in this milestone — reuses the authoritative M15.2C fixture `20260818-183933-db74b2`. Build 0/0; **504/504 tests** (+13 `TokenEfficiencyMetricsTests.cs`, offline: activity/fresh/ratio calcs, exact M15.2C fixture 44/14325/1125952/52193 → activity 1178189 / fresh 52237 / ratio ≈0.95566 / TotalTokens 14369, executor flow, cache-absent null semantics never 0, TotalTokens unchanged, OpenCode/Codex → null, run-level ratio from aggregates 1700/3000 ≈0.5667 NOT avg 0.6, order independence, provider-execution.json serialization, package gains no derived/cache fields). Interpretation (M15.2C ClaudeCode/Security): ~95.6% of input-side context-token activity was cache reads — NOT "95.6% cheaper", NOT "% of repo cached"; "Claude consumed 1.17M tokens" only if explicitly qualified as context-token activity; `TotalTokens` 14,369 remains the M12.1 total. No ADR — derived metrics, no architecture decision. | Raw authoritative categories must stay the single source of truth; the derived metrics exist only to make them interpretable ("where is the agent spending context/token activity?") without redefining M12.1 meaning or inventing cost — a durable semantic that later cost/optimization milestones can build on. | `MILESTONE-015.2D` |
| D-103 | 2026-08-18 | Claude Code Complete Token Telemetry (M15.2C): preserve the authoritative cache categories from the `claude -p --output-format json` result envelope (`usage.cache_read_input_tokens` / `usage.cache_creation_input_tokens`) as SEPARATE, provider-neutral operational telemetry — measurement only, reusing the M12.1 surface. `ClaudeCodeOutputExtractor.ParseUsage` maps both (numeric-only; missing/non-numeric → null) into `ClaudeCodeUsage` (+2 nullable fields); `TotalTokens` UNCHANGED = `SumKnown(Input, Output)` — cache tokens are NEVER added to it (backward-compatible, historical meaning preserved; example 179+17605=17784, not 81461). `ClaudeCodeEvidenceProvider` stamps `Evidence` + metadata `cacheReadInputTokens`/`cacheCreationInputTokens` (when present); generic `Evidence`/`ProviderExecutionRecord` gain `CacheReadInputTokens?`/`CacheCreationInputTokens?` (no Claude-specific types leak past infrastructure); `EvidenceAcquisitionExecutor.Record` lifts them (single telemetry path). Run-level aggregation ADDED (clean, minimal): `ProviderExecutionReport` gains `TotalCacheReadInputTokens?`/`TotalCacheCreationInputTokens?` via the existing `SumKnown`; `KnownTokenExecutionCount` STILL means M12.1 input/output-known executions — cache-only usage never counts. External `engineering-review-package.json` must NOT gain cache fields, so `EngineeringReviewPackageBuilder.PackageExecutionView` projects the report with cache nulled (serializer omits nulls) → package stays `schemaVersion` **1.1**; cache telemetry lives in `provider-execution.json` (+ `run.json`) only. NO derived metric (`EffectiveTokens`/`ContextTokens`/cost/billable) — we don't yet know which semantic is useful, so persist the raw authoritative categories first; no pricing tables, no CostUsd. OpenCode/Codex remain honestly null (no machine-readable usage; nothing estimated/tokenized/scraped). Build 0/0; **491/491 tests** (+12 `ClaudeCodeCacheTokenTelemetryTests.cs`, offline scripted fakes: cache_read/cache_creation mapping to evidence+metadata+record, both propagate, missing/explicit-null stay null never 0, TotalTokens excludes cache, input/output unchanged, provider-execution.json serialization incl. run totals, package gains NO cache fields, SumKnown run totals, cache-only never changes KnownTokenExecutionCount). REAL validation (run `20260818-183933-db74b2`, Security, OpenCode+Codex+ClaudeCode, MaxConcurrency 3, `RedirectToService`; runtime-only `Evidence__OpenCode__TimeoutSeconds=300`/`Evidence__ClaudeCode__TimeoutSeconds=300`, shipped defaults unchanged): **3/3 success** — OpenCode 3m18.6s (10 obs, tokens null), Codex 1m43.8s (6 obs, tokens null), ClaudeCode 2m40.0s (6 obs, envelope-authoritative: input 44/output 14325/total 14369/cacheRead 1125952/cacheCreation 52193); run `TotalCacheReadInputTokens 1125952`/`TotalCacheCreationInputTokens 52193`, `KnownTokenExecutionCount 1`, `TotalTokens 14369`; package 1.1 with NO cache fields, no credentials, no orphan processes. Cache-dominated context confirmed (~1.13 M cached vs 44 new). No ADR — additive telemetry, no architecture decision. | The cache categories are authoritative facts in the envelope we already consume; dropping them hid that a real agentic call's context is cache-dominated (M15.2A smoke: ~50k cached vs 6 new). Preserving them as SEPARATE generic telemetry keeps M12.1 `TotalTokens` semantically intact and lets a future milestone decide the right cache-aware interpretation rather than baking one in now. | `MILESTONE-015.2C` |
| D-102 | 2026-08-18 | Agentic Provider Timeout Precedence Fix (M15.2B): the executor's hard per-step cap `EvidenceOptions.ProviderTimeout` (120 s default) was NOT bound from config (CLI/API built `EvidenceOptions` without it), so every real agentic step was cut at 120 s regardless of each provider's `TimeoutSeconds` — the M15.2A live run proved it by timing out all 3 providers at exactly 00:02:00 despite ClaudeCode configured 300 s. Fix = per-step precedence at the acquisition boundary: `stepTimeout = provider.Metadata.Timeout ?? _options.ProviderTimeout` in `EvidenceAcquisitionExecutor` (used by `CancelAfter` and the timeout log/failure message), with `EvidenceProviderMetadata` gaining a nullable `Timeout` that the three agentic providers stamp from their own options (`Timeout = _options.Timeout`) so the generic executor never branches on provider names. The global `Evidence:Execution:ProviderTimeout` is now a REAL config-bound fallback: `CouncilOptions.ProviderTimeout` (default 120 s) is passed into `EvidenceOptions` via DI, and both CLI (`CliArgs.ParseProviderTimeout`) and API (`EvidenceConfig.ParseProviderTimeout`) parse it (seconds, default 120); `ProviderTimeout: 120` shipped in both appsettings. Semantics preserved: provider timeout ⇒ `ErrorCategory=Timeout`, run cancellation propagates as `OperationCanceledException` (never converted), failure stays failure; MaxConcurrency untouched. All `[CP]` diagnostic probes removed (`Cli/Program.cs` ~109-111; `AnalysisPipeline.cs` `Cp` + 6 call sites; grep clean). No contract change: `schemaVersion` 1.1, package records carry NO timeout field, no new artifact/telemetry model. Build 0/0; **479/479 tests** (+11 `ProviderTimeoutPrecedenceTests.cs`, offline scripted fakes — provider-over-global precedence, global fallback, per-provider metadata propagation OpenCode/Codex/ClaudeCode, timeout ⇒ Timeout category, cancellation stays cancellation, parallel independent timeouts, no timeout leak into package, defaults + shipped config keys bound). REAL validation (run `20260818-181449-72ca35`, Security, OpenCode+Codex+ClaudeCode, MaxConcurrency 3, `RedirectToService` — same target that was 0/3 in M15.2A): **2/3 success**; ClaudeCode (own 300 s) ran **198.6 s** and completed — the old 120 s cap would have terminated it (acceptance met); Codex (own 120 s) completed 104.2 s; OpenCode (own 120 s) timed out at 00:02:00; `TotalInputTokens 179`/`TotalOutputTokens 17605`/`TotalTokens 17784`/`knownTokenExecutionCount 1`; 15 observations → 8 findings (C0/H1/M6/L1), 2 multi-provider, 0 contradictions, 0 retries; one `schemaVersion` 1.1 package, no timeout field, no secrets, no orphan processes. No ADR — config binding + precedence inside the existing executor, no architecture decision. | A per-provider timeout is the operator's intent and must be honored at the acquisition boundary; the 120 s global cap only exists as a safety fallback for providers without their own budget. Binding it from config makes the fallback explicit and inspectable, and carrying the effective timeout on provider metadata keeps the generic executor name-free (same pattern as D-053). | `MILESTONE-015.2B` |
| D-101 | 2026-08-18 | Agentic Token Usage Telemetry (M15.2A): capture per-execution token usage for the three agentic runtimes whenever the runtime reports AUTHORITATIVE usage in the machine-readable output the adapters already consume — measurement only, reusing the M12.1 provider-neutral surface (`Evidence.InputTokens/OutputTokens/TokensUsed`, `ProviderExecutionRecord`, run report `TotalInputTokens/TotalOutputTokens/TotalTokens/KnownTokenExecutionCount`). Per-runtime reality inspected: **ClaudeCode** (`claude -p --output-format json`) result envelope carries authoritative `usage.input_tokens`/`usage.output_tokens` (cumulative for the whole call — the top-level agent loop incl. internal Read/Glob/Grep; no subagents) → MAPPED via `ClaudeCodeOutputExtractor.TryExtractResult` (+ new `ParseUsage` + internal `ClaudeCodeUsage(Input?,Output?)` record with `TotalTokens` = sum when ≥1 side known), provider stamps `Evidence` + metadata `inputTokens/outputTokens/totalTokens`; **OpenCode** (`opencode run`) and **Codex** (`codex exec`) captured stdout is final-message text only — no machine-readable usage → tokens stay NULL (documented gap, nothing fabricated, adapters unchanged). Unknown is never 0 (M12.1 `SumKnown`); no new telemetry model, no new artifact, no package field, `schemaVersion` stays **1.1** (token fields appear only inside the pre-existing `providerExecution` section, never in `findings`); no provider-specific type leaks past the adapter boundary. Build 0/0, **468/468 tests** (+12 `AgenticTokenTelemetryTests.cs`, offline scripted fakes: ClaudeCode mapping/nulls, OpenCode/Codex null behavior, unknown-never-zero aggregation, evidence→record pipeline flow, `KnownTokenExecutionCount`, provider-execution.json serialization, package 1.1 contract, no credentials in telemetry). Cache-token fields and `total_cost_usd` in the ClaudeCode envelope are deliberately NOT mapped (out of scope). No ADR — adapter-internal mapping, no architecture decision. | The M15.2 run showed agentic providers burn real budget with zero visibility; Claude Code's CLI already exposes authoritative session usage in the envelope the adapter consumes, so the honest, minimal move is to lift the two provider-neutral counts — while OpenCode/Codex captured output genuinely carries none, so fabricating numbers or switching output modes would violate M12.1 null-never-zero and add scope. | `MILESTONE-015.2A` |
| D-100 | 2026-08-13 | Post-run Output Quality (M15.2): evaluated the FIRST full 3×7 parallel Council run (`20260813-230240-b3e36e`, RedirectToService, OpenCode+Codex+ClaudeCode × 7 disciplines). Real results: 21 acquisition steps, 11 success / **10 timeouts** (Architecture & Testing **0/3** coverage → zero findings), 82 observations (OpenCode 51 / Codex 18 / ClaudeCode 13), 29 findings (critical 1 / high 6 / medium 13 / low 9), 6 multi-provider agreements, 0 contradictions, one `schemaVersion` 1.1 package, health Critical, no provider secrets, no orphan processes. **VERDICT: B (conditional)** — content is verbatim-accurate (top findings verified against committed `HEAD`, commit `4a7d279aaa99`) and actionable, but two structural defects must be fixed before the Council signs off: (1) The summary reports *"No Architecture / No Testing issues identified"* as validated strengths despite **0% evidence coverage** — absence of evidence must be surfaced as a coverage failure, never as an all-clear; (2) **9/29 findings (~31%) duplicate 3 root causes** (RequestFilter NRE ×4 across reliability/observability/codeQuality; `/readyz`-vs-`/healthy` duplicate ×2; "Magic strings for route names in RedirectsController" ×3 identical titles) — no intra-analyzer or cross-discipline dedup. One finding (CQL-7fe95e8d97) is **disputed/unreproducible**: committed code does use `_realmProvider` yet the finding claims it is unused — the run-time worktree differed from both HEAD and today's worktree (snapshot drift caught live), so its input is unreplayable. M15.3 TODO (deferred, not built): acquisition robustness (agentic Architecture/Testing steps need larger budgets/retries/repair or smaller steps), snapshot pinning (tree SHA / `git-archive` copy) so findings are replayable, reconciler dedup + per-discipline evidence-coverage in the package with any "no issues" statement gated on coverage > 0. NO production code, contract, or ADR changed in this milestone. | The Council self-assessment *"output quality is acceptable"* holds for finding **content** (verbatim-faithful, coherent line references, cross-provider consensus on 6 shared issues) but NOT for its **self-reported coverage**: reporting "no issues" for disciplines where all acquisitions timed out is a validation-layer defect, and counting 9 duplicate findings as distinct inflates the headline number ~31%. Both are fixable data/modeling gaps, not analyzer failures — hence B rather than any build-blocker, and no code change made in this evaluation milestone. | `MILESTONE-015.2` |
| D-099 | 2026-08-12 | Parallel Agent Execution (M15.1): install a BOUNDED, OPT-IN concurrency for INDEPENDENT acquisition steps. `EvidenceAcquisitionExecutor.ExecuteAsync` now runs steps through a `SemaphoreSlim(maxConcurrency)` (never unbounded Tasks) with `maxConcurrency = Math.Max(1, Evidence:Execution:MaxConcurrency)`; each step keeps its own provider/request/timeout-CTS/per-step locals; the `WaitAsync` is OUTSIDE the try/finally so a cancelled waiter never releases a permit it did not acquire; final `Evidence`/`records` aggregate STRICTLY IN PLAN ORDER after all tasks complete so `provider-execution.json`, observations, package JSON and Markdown stay deterministic. Default `MaxConcurrency = 1` (strictly sequential — identical behavior to pre-M15.1), bound in both the CLI and API `Program.cs`. NO contract change: `engineering-review-package.json` stays `schemaVersion 1.1` and gets NO `maxConcurrency` field; consumer-DTO gate green. Build 0/0; **456/456 tests** (+15, `ParallelAcquisitionTests.cs`, fully deterministic `TaskCompletionSource`/instant fakes, offline) proving sequential default, `MaxConcurrency=1..3` never exceeds the bound, `Continue`/`FailRun` preserved under a parallel executor, cancellation propagates (queued steps never start) while timeout stays distinct and retriable, no step duplication/plan-order reordering, contract unchanged. REAL verification (run `20260812-222936-2229b2`, OpenCode+Codex+ClaudeCode / Security, fixture `m14-1-council-smoke-repo`, `MaxConcurrency=3`): all three providers started within **87 ms** (22:29:36.58→22:29:36.67) → true overlap confirmed at the process level too (opencode pid 40884 + codex node 32632/codex.exe 24764 alive together ~22:29:37→22:30:06); acquisition wall-clock ~57.2 s (22:29:36.58→22:30:33.79) vs ~150.36 s M14.1 sequential baseline ≈ **62% reduction**; 3/3 success, 0 failures, 0 timeouts, 0 retries; 21 obs → 13 raw → 12 consolidated findings, 3 multi-provider, 0 contradictions; exactly ONE package `schemaVersion 1.1`; no orphan processes on Windows. `provider-execution.json` `totalDuration` remains the M12.1 SUM aggregator (00:02:20.04) — reported beside externally-measured wall-clock (~59 s), never confused with it. The temporary `[CP]` stderr diagnostic scaffold (added earlier to observe overlap) was REMOVED after the run with build/tests re-verified. **Next: M15.2** (not started). | Parallelism had to be a bounded, opt-in EXECUTION change only: (1) `1` as default keeps every existing sequential run byte-identical, satisfying the "default-don't-break-it" rule while a 3-agent council still gets the ~62% wall-clock win; (2) a `SemaphoreSlim` bound (not `Task.WhenAll` over all steps) prevents unbounded process/port/resource fan-out across repositories and retries; (3) session-order aggregation keeps every artifact deterministic so downstream consumers, `run.json` replays, and Markdown do not depend on nondeterministic completion order; (4) the package contract is untouched because parallelism is an execution concern, not a data contract (the consumer gate stays green). Rooted in long-standing design notes (ADR-006 `MILESTONE-005`: "factored so parallel is a one-method change", ADR-009 D-050 removed the process-global log so telemetry never interleaves). | `MILESTONE-015.1` |
| D-098 | 2026-08-12 | M14.4 real-data validation: **skip M14.5** (Decision A). No git repository exists in this working tree; the exact documented M14.1 run (`20260811-011804-d3eb0a`) is not persisted in `outputs/`, only three smaller/partial `m14-1-council-smoke-repo` runs. More importantly, this is structural, not a missing-artifact problem: every real Council run is agentic-provider-only (`OpenCode`/`Codex`/`ClaudeCode`), and `D-089` already excludes `ProviderType.Agentic` from `ProviderComparisonBuilder` — so `CalibrationDiagnosticsReport` (and therefore `ObservationTypeDisagreement`, which has no finding-level fallback unlike `LocationDisagreement`) is never populated for any real Council run. `SemanticReconciliationBuilder.IsCandidate` therefore selects **zero** candidates from any real persisted data, always, under the current architecture — confirmed empirically (`calibrationDiagnostics: false` on all 3 real runs found). Falling back to the deterministic M14.1-shaped fixture (per the task's own Step 1 fallback) reproduces the documented 2 candidates, but invoking the real `LlmSemanticReconciliationReviewer` needs an authenticated `ILlmChatClient` (`ANTHROPIC_API_KEY`/`OPENAI_API_KEY`); neither was present and no other credential path existed, so the real reviewer was not invoked and no decision was fabricated. With real candidate count fixed at 0, `DifferentIssues` is 0 for all real data regardless of reviewer availability — M14.5 (safe splitting of `DifferentIssues` findings) has no real case to act on today. Zero production code changed; build/tests unchanged from M14.4's own verification (441/441). Next: M15.1 Parallel Agent Execution. | The decision must be evidence-based, and the strongest available evidence is that the candidate-selection precondition (`ObservationType` disagreement) cannot occur at all for real, agentic-only Council runs under the current, unmodified architecture — not merely that a specific run's artifacts were missing. Building M14.5's splitting logic now would be speculative against a pipeline stage that currently never fires in production. This does not reopen or change the Agentic/`ProviderComparison` exclusion decided in `D-089` — extending calibration diagnostics to Agentic providers is out of this task's scope and is not proposed here. | `MILESTONE-014.4` |
| D-097 | 2026-08-12 | Targeted Semantic Reconciliation (M14.4), acting on M14.3's Decision B: an OPTIONAL, narrowly-scoped semantic review for EXACTLY the ambiguous subset — `SemanticReconciliationBuilder.IsCandidate` selects a finding only when `CouncilAssessment.Type == AgreementWithDifferences` AND `Differences` contains `ObservationType`; everything else (`SingleSource`/`StrongAgreement`/`PotentialConflict`/severity-only/location-only/unassessed) bypasses review entirely, verified against the exact 6-finding fixture the milestone spec describes (2/6 invoke the reviewer). New contract: `SemanticReconciliationDecision {SameIssue, DifferentIssues, Inconclusive}` + `SemanticReconciliationResult {Decision, Reason}` (`Core.Domain`, no scores/probabilities/rankings/votes); `ISemanticReconciliationReviewer` + a MINIMAL, finding-scoped `SemanticReconciliationRequest` (id/title/category/providers/attributed observations only — never the repository, other findings, or the package) (`Core.Abstractions`). Real implementation `LlmSemanticReconciliationReviewer` (`Infrastructure.Reconciliation`) depends ONLY on the EXISTING generic `ILlmChatClient` seam (the same transport Claude/OpenAI already use, never a specific provider) and reuses the EXISTING `StructuredJsonExtractor` — not a second LLM framework (no retries, no repair, one attempt). All three decisions share ONE code path: the finding is NEVER mutated beyond attaching `.SemanticReview` — `DifferentIssues` is recorded as advisory-only and does NOT split the finding in M14.4 (splitting would require re-deriving severity/confidence/provenance per sub-group, effectively a new de-merge rule; explicitly deferred to a future milestone per ADR-025). Reviewer failure (timeout/malformed/unavailable) is caught per-candidate and becomes `Inconclusive` with a secret-safe reason; a SECOND defensive catch around the whole enrichment stage in `AnalysisPipeline` guards even an unexpected bug there — only genuine run cancellation propagates; semantic review can NEVER fail the run. Disabled by default (`Council:SemanticReconciliation:Enabled=false`); `AnalysisPipeline` gained two OPTIONAL trailing constructor parameters (reviewer + options) so all 16 existing direct-construction test files kept compiling and passing UNCHANGED. A real offline smoke with the feature at its default produced a package byte-identical in shape to the M14.3 smoke (0 `semanticReview` occurrences) — proving disabled behavior is unchanged. Additive package exposure: `findings[].semanticReview` (`schemaVersion` unchanged, **1.1**); compact `Semantic Review: <Decision>` / `Reason: …` lines under the existing Markdown Council Assessment block; consumer DTO gained `SemanticReviewContract`; gate green. Tests use a scripted `ISemanticReconciliationReviewer` fake plus the EXISTING `ScriptedLlmClient` — fully offline, no network/credentials/real agent runtime. Build 0/0, **441/441 tests** (+21). | The M14.3 evaluation is only useful if it's ACTED on precisely — reviewing anything beyond the 2/13 evidence-identified ambiguous class would silently reintroduce a general LLM judge, undoing the very deterministic-reconciliation argument M14.3 made. Splitting `DifferentIssues` findings was deliberately NOT attempted: correct splitting needs its own reconciliation-adjacent design (how severity/confidence/provenance are re-derived for each half), and "prefer a safe advisory result over risky mutation" outweighs shipping a small, risky mutation just to look more complete. Reusing the existing `ILlmChatClient`/`StructuredJsonExtractor` seams (rather than inventing new ones) keeps this the platform's smallest possible non-deterministic boundary — and it stays fully offline-testable because provider identity is DI composition, never domain logic. | `MILESTONE-014.4`, `ADR-025` |
| D-096 | 2026-08-12 | Council Assessment Evaluation (M14.3): evaluated the M14.2 deterministic Council against the real M14.1-shaped fixture (13 consolidated findings, unmodified rules) — 7 SingleSource / 1 StrongAgreement / 5 AgreementWithDifferences / **0 PotentialConflict**; difference-type totals across the 5 AgreementWithDifferences findings: Severity 2, ObservationType 2, Location 5, Contradiction 0. Added `CouncilAssessmentSummary` (`Core.Domain`: `SingleSourceCount`/`StrongAgreementCount`/`AgreementWithDifferencesCount`/`PotentialConflictCount` + computed `TotalAssessed`) with ONE authoritative calculation — `CouncilAssessmentBuilder.Summarize(findings)` purely aggregates each finding's already-computed `CouncilAssessment.Type` (unassessed findings excluded, never guessed); `EngineeringReviewPackageBuilder` calls it exactly once over the same assessed findings it produced via `Apply`; no exporter recomputes it independently (a test asserts the package's stored summary equals an independent `Summarize()` call over the same findings). Exposed additively: `councilAssessmentSummary` in `engineering-review-package.json` (`schemaVersion` unchanged, **1.1**), compact `## Council Assessment` block in `engineering-review.md`, consumer DTO gained `CouncilAssessmentSummaryContract`, gate green — verified both on the fixture and on a real offline `review --provider Mock` run. **DECISION: B** — deterministic reconciliation is sufficient for MOST findings (0 conflicts; severity/location-only differences are already safely resolved — "take the highest severity", location variance is expected line-number noise for the same issue), but a SMALL, narrowly-identifiable subset is genuinely ambiguous: the 2 findings (2/13) whose `Differences` include `ObservationType` — deterministic rules can observe THAT the type differs across providers but cannot determine WHETHER that means "same issue, different label" (harmless) or "two distinct issues merged under one consolidated finding" (a possible false merge hiding a real, separate issue). Future semantic-review candidates are scoped to EXACTLY that subset (ObservationType-disagreement findings) — nothing broader. No classification rule was changed to shape these numbers; no next layer (semantic reconciliation / LLM judge / voting / ranking / weighting) was implemented. Build 0/0, **420/420 tests** (+8, offline only, no OpenCode/Codex/ClaudeCode execution required). | A decision about whether to add a semantic layer must be evidence-based, not speculative — replaying the real M14.1 run's finding shape (rather than a synthetic scenario) grounds the decision in what an actual multi-agent Council run produced. Zero conflicts across 13 real findings is strong evidence the deterministic rules are not systemically failing, but the ObservationType-disagreement cases expose a real, narrow blind spot (type disagreement + location disagreement together, unlike severity-only or location-only differences, cannot be resolved by any existing deterministic rule) that is worth naming precisely now so a future, OPTIONAL, narrowly-scoped semantic-review layer — if ever built — targets exactly the cases that need it rather than second-guessing findings that are already handled well. | `MILESTONE-014.3` |
| D-095 | 2026-08-12 | Deterministic Council assessment (M14.2): new `ReconciliationAssessment` (`Type`: `SingleSource`/`StrongAgreement`/`AgreementWithDifferences`/`PotentialConflict`, `SupportingProviders`, `AgreementCount`, `Differences`, `ContradictionReasons`) + pure `CouncilAssessmentBuilder.Apply/Assess` (`Core.Application`) computed ONLY from EXISTING authoritative data — the reconciler's own `SupportingProviders`/`AgreementCount`/`SeverityRange`/`ContradictionReasons` on the consolidated `Finding`, plus the M12.3/M12.4 `CalibrationDiagnosticsReport` (observation-type/location signals) when present. NO LLM call, NO re-reconciliation, NO voting, NO provider ranking/weighting, NO new Council architecture. `PotentialConflict` requires an EXPLICIT reconciler contradiction reason that is NOT already explainable as a severity/file/location difference (those count as `AgreementWithDifferences` instead) — never inferred from a mere severity/location/observation-type difference or from a provider simply not reporting the finding, so zero conflicts is a valid, expected run outcome. `EngineeringReviewPackageBuilder` stamps `Finding.CouncilAssessment` on CONSOLIDATED findings only (`CouncilAssessmentBuilder.Apply(run.Findings, run.CalibrationDiagnostics)`); `Appendix.RawFindings` is untouched. Exposed additively as `findings[].councilAssessment` in `engineering-review-package.json` (camelCase enum strings; `schemaVersion` stays **1.1**), projected in `engineering-review.md` (`Council Assessment: <Type>` / `Supporting Providers:` / `Differences:` / `Explicit Contradictions:`), consumer DTO gained `CouncilAssessmentContract`, and the committed integration sample was regenerated. Verified two ways: (1) a deterministic fixture replays the EXACT finding shape of the real M14.1 Council run (13 findings: 7 exclusive, 2×3-provider, 4×2-provider; severity 2/observation-type 2/location 5 disagreements; contradictions 0) and the assessment layer reproduces 7 SingleSource/1 StrongAgreement/5 AgreementWithDifferences/**0 PotentialConflict**, matching M14.1's own `contradictions=0` exactly; (2) a real offline `review --provider Mock` run over an evaluation-dataset fixture produced an actual package with `councilAssessment` on all 7 consolidated findings (`singleSource`, `agreementCount=1`) and the matching markdown block. Build 0/0, **412/412 tests**; internal diagnostic names (`severityDisagreement`/`observationTypeDisagreement`/`locationDisagreement`/`calibration`) asserted to never leak into the package. | Every fact the assessment reports must already have one authoritative source in the run (the reconciler for agreement/severity/contradictions, M12.3/M12.4 for observation-type/location) so the classification can never contradict reconciliation or invent a signal; treating "different" and "conflicting" as distinct concepts prevents the common failure mode of over-reporting disagreement as conflict; replaying the real M14.1 run's exact shape is stronger verification than a synthetic fixture because it proves the assessment layer's zero-conflict outcome matches what the live multi-agent run actually produced. | `MILESTONE-014.2` |
| D-094 | 2026-08-10 | First real Multi-Agent Engineering Council run (M14.1) + fix of the M14.1 hang. One AnalysisRun executed **OpenCode + Codex + ClaudeCode** over the same Security discipline and repository (`m14-1-council-smoke-repo`) and consolidated through the EXISTING deterministic reconciler — NO new architecture, NO new ADR; the reconciler remains authoritative (agreement/disagreement numbers reported as-is, nothing forced). Verified facts (run `20260811-011804-d3eb0a`): 3/3 providers succeeded (OpenCode 32.89 s/8 obs/6 raw, Codex 59.73 s/6 obs/5 raw, ClaudeCode 57.73 s/10 obs/4 raw); 24 observations → 15 raw findings → 13 consolidated findings; supported by all 3 = 2, exactly 2 = 4, exclusive = 7 (OpenCode 3, ClaudeCode 3, Codex 1); severity disagreements 2, observation-type 2, location 5, contradictions 0; package `schemaVersion **1.1**`, exactly one package, provider provenance preserved, no secrets in artifacts, no orphan processes. HANG ROOT CAUSE (not an adapter bug): the host resolved `appsettings.json` against the process **cwd**, so launching from the repo root never loaded `Evidence:OpenCode:Port=0`; OpenCode then ran WITHOUT `--port`, competed for the shared default server an interactive session held, and queued to the 120 s timeout. Fix: `Cli/Program.cs` `ConfigureAppConfiguration((_, config) => config.SetBasePath(AppContext.BaseDirectory))` — the CLI's own appsettings.json always loads regardless of cwd and every OpenCode run emits a concrete isolated `--port <n>` (verified port **58041**). Deterministic regression guard `Cli_appsettings_keeps_the_m141_isolated_port_and_explicit_model` (Tests/OpenCodeModelSelectionTests.cs) pins the shipped config (`Port=0`, model, `Enabled=false`); effective config re-verified from the repo root; `DEEPSEEK_API_KEY` checked presence-only (absent from User/Machine env, value never printed). Build 0/0, **398/398 tests**, package stays **1.1**. | The hang reproduced a CONFIG-LOADING trap (cwd-dependent discovery), not a process-runner defect — the fix is anchoring config to the app base directory plus a deterministic config-contract test, not an architectural change; the council seam already isolated each agentic run once `--port` was actually passed. Isolated `--port` is the right default for agentic runs (a busy shared server must not queue the run); the deterministic reconciler is unchanged and remains the single authority for which sources agree. | `MILESTONE-014.1` |
| D-093 | 2026-08-09 | Claude Code agentic adapter (M13.5): a THIRD real external coding-agent runtime behind the ADR-021 Agentic contract, proving the contract is runtime-agnostic (OpenCode + Codex + Claude Code share one seam, same downstream). `ClaudeCodeEvidenceProvider` (`ProviderType = Agentic`, `Name = ClaudeCode`, Discipline-scoped, `Version = "claudecode-adapter-v1"`, `ProviderId = "claudecode"`) launches the `claude` executable against the repository root, hands it ONE discipline-specific READ-ONLY analysis instruction, unwraps the CLI's own `--output-format json` result envelope (via the minimal `ClaudeCodeOutputExtractor` — ONE documented `result` field only, then the existing `LlmEvidenceResponseValidator`; NOT a new parser, no prose salvage, no repair call), and converts the `observations` envelope into existing `Evidence` — downstream never learns Claude Code exists; no `ClaudeCodeObservation`/`Finding`/`Result` types. NAMING: `ClaudeCode` (agentic CLI runtime) ≠ `Claude` (direct Anthropic API provider) — unambiguous providers, never conflated. Execution is isolated behind a small purpose-built `IClaudeCodeProcessRunner`/`ClaudeCodeProcessRunner` mirroring the OpenCode/Codex seam (NOT a generic executor): `ProcessStartInfo { UseShellExecute = false }` + `ArgumentList`, `WorkingDirectory = repository root`, bounded output, exit code, timeout, cancellation, and **complete process-tree** termination (M13.3A lesson). Claude Code invocation is `-p --output-format json --no-session-persistence --tools "Read,Glob,Grep" [--model <model>] -- "<instruction>"` — `--tools "Read,Glob,Grep"` is the CLI's own supported read-only mechanism (write tools removed from the built-in set; the prompt is the second boundary; NOT an OS sandbox — unsupported on Windows), `--no-session-persistence` keeps it stateless, and `--` stops the VARIADIC `--tools <tools...>` from swallowing the prompt (verified against real v2.1.223 — without it the CLI fails "Input must be provided either through stdin or as a prompt argument"). NO API-KEY OPTION anywhere: Claude Code authenticates through its own login (`claude auth` / the configured account), the Council never reads/stores/forwards a credential. Optional model (`ClaudeCodeOptions.Model`, e.g. `claude-sonnet-5` or alias `sonnet`) → `--model <id>` via `ArgumentList`, unset ⇒ Claude Code default, never mandatory/guessed/hardcoded; when set stamped as `Metadata["model"]` telemetry while `ProviderVersion` stays the adapter version. Failure semantics match OpenCode/Codex: envelope failures (`is_error`, non-envelope, missing/empty `result`) and malformed output ⇒ categorized `SchemaValidation` (existing validator, no repair call), non-zero exit ⇒ `ProcessError` with bounded stderr, timeout terminates the complete process tree ⇒ `Timeout` failure, run/user cancellation terminates and propagates `OperationCanceledException`. `ContextFingerprint` stays `""` — never fabricated; M12 comparison still excludes agentic. Real smoke proven: Claude Code CLI **2.1.223** authenticated via its own login, fixture repo → Evidence → **2 Security observations** (`HardcodedSecret` critical + `BrokenAuthentication` high) → **2 findings** → package (`schemaVersion **1.1**`, no `providerComparison`, no `model` field) in ~31 s with **no orphan processes**. OpenCode + Codex + ClaudeCode real-process tests share one NON-PARALLEL xUnit collection (`ProcessRunnerTests`). | Claude Code is the third runtime to prove the agentic contract is genuinely runtime-agnostic; the CLI's own `--tools` restriction gives a runtime-enforced read-only boundary (no OS sandbox exists on Windows), and reusing the existing envelope + validator means the adapter stays minimal — the only Claude-Code-specific surface is the ONE documented `result` envelope field, never a new parser. | `ADR-024` |
| D-092 | 2026-08-09 | Codex agentic adapter (M13.4): a SECOND real external coding-agent runtime behind the ADR-021 Agentic contract, proving the contract is runtime-agnostic. `CodexEvidenceProvider` (`ProviderType = Agentic`, `Name = Codex`, Discipline-scoped, `Version = "codex-adapter-v1"`) launches the `codex` executable against the repository root, hands it ONE discipline-specific READ-ONLY analysis instruction, validates the structured JSON with the SAME `LlmEvidenceResponseValidator`, and converts it into existing `Evidence` — downstream never learns Codex exists; no `CodexObservation`/`Finding`/`Result` types. Execution is isolated behind a small purpose-built `ICodexProcessRunner`/`CodexProcessRunner` mirroring the OpenCode seam (NOT a generic executor): `ProcessStartInfo { UseShellExecute = false }` + `ArgumentList`, `WorkingDirectory = repository root`, bounded output, exit code, timeout, cancellation, and process-tree termination. Codex invocation is `exec -s read-only --ephemeral --skip-git-repo-check [-m <model>] "<instruction>"` — `-s read-only` enforces the read-only boundary AT THE CODEX SANDBOX level (stronger than OpenCode's instruction-only boundary), `--ephemeral` keeps it stateless, `--skip-git-repo-check` keeps it usable on plain directories. NO API-KEY OPTION anywhere: Codex authenticates through its own login (ChatGPT account / `codex login`), the Council never reads/stores/forwards a credential. Optional model (`CodexOptions.Model`, e.g. `gpt-5.4-mini`) → `-m <model>` via `ArgumentList`, unset ⇒ Codex default, never mandatory/guessed/hardcoded; when set stamped as `Metadata["model"]` telemetry while `ProviderVersion` stays the adapter version. Failure semantics match OpenCode: malformed output ⇒ categorized `SchemaValidation` (existing validator, no repair call), non-zero exit ⇒ `ProcessError` with bounded stderr, timeout terminates the process ⇒ `Timeout` failure, run/user cancellation terminates and propagates `OperationCanceledException` (never an ordinary failure). `ContextFingerprint` stays `""` — never fabricated. Config `Evidence:Codex { Enabled: false, Executable: "codex", TimeoutSeconds: 120, Model: "" }`, disabled by default, `--provider Codex` opts in. The OpenCode + Codex real-process tests share one NON-PARALLEL xUnit collection (`ProcessRunnerTests`) so their short-lived `cmd /c ping` orphan-survivor probes never observe each other's children. Real smoke run `20260809-052551-9bb061` (native Codex CLI, fixture repo, model `gpt-5.4-mini`): ~61 s, Evidence → 3 Security observations → 3 findings (2 High, 1 Medium) → package (`schemaVersion **1.1**`), `contextFingerprint ""`, **no orphan processes**; the live Stripe key was redacted by Codex itself (`sk-live-…`) and no adapter credential ever reached telemetry/artifacts. | Adding a second runtime is the strongest possible proof the agentic contract (ADR-021) is provider-neutral, and Codex brings a genuinely stronger read-only boundary (`-s read-only` sandbox) plus authentication fully outside the Council — no key handling to get wrong. Copying the runner seam (rather than a shared base class) keeps each adapter readable and independently testable; the real-process tests must not run in parallel or one test's orphan-survivor probe sees the other's still-running child. | `ADR-023` |
| D-091 | 2026-08-09 | OpenCode model selection (M13.3): optional `Evidence:OpenCode:Model` in OpenCode's `provider/model` format is passed to the process as `--model <id>` via the existing safe `ArgumentList` invocation (`UseShellExecute = false`, never a shell string); the instruction stays the LAST argument so the runner/telemetry contract is unchanged. When set, `OpenCodeEvidenceProvider` stamps `Metadata["model"]` on evidence (built before the init-only `Evidence.Metadata` initializer) and logs it; when unset, model identity stays absent (never `"unknown"`). `ProviderVersion` remains the adapter version — model is configuration, never domain logic, never hardcoded, never a credential (keys live in OpenCode's own auth, e.g. `DEEPSEEK_API_KEY`). A real smoke run (OpenCode **1.18.15**, model `opencode/deepseek-v4-flash-free`, fixture repo) succeeded: ~27.6 s, Evidence → 5 observations → 4 Security findings → package (`schemaVersion **1.1**`), `contextFingerprint ""`, **no orphan processes**. An observed hang was investigated and root-caused as a STALE CLI BINARY (built pre-M13.3, never passed `--model`, OpenCode fell back to its billing-blocked default model) — NOT a runner defect; process-tree termination was separately proven sound and locked in with 2 new Windows-safe regression tests in `OpenCodeProcessRunnerTests.cs` (timeout and cancellation terminate the complete `cmd /c ping` tree). | Model identity is a runtime fact the adapter must forward explicitly — guessing or hardcoding it would be dishonest; the `ArgumentList` `--model` passthrough preserves the M13.2 "no shell strings / no name branching" rules. The hang reproduced a real operational trap (running an out-of-date binary) rather than an adapter bug, so the fix is a documented rebuild rule plus regression tests, not an architectural change. | `MILESTONE-013.3` |
| D-090 | 2026-08-08 | OpenCode agentic adapter (M13.2): the first REAL external coding-agent runtime behind the ADR-021 Agentic contract. `OpenCodeEvidenceProvider` (`ProviderType = Agentic`, `Name = OpenCode`, Discipline-scoped, `RequiresAnalyzerInstructions = true`, `SupportsRepositoryWideAnalysis = false`) launches the `opencode` executable against the repository root, hands it ONE discipline-specific READ-ONLY analysis instruction, validates the structured JSON with the SAME `LlmEvidenceResponseValidator` as the LLM providers, and converts it into existing `Evidence` — downstream (interpreter → observations → analyzers → reconciliation → package) never learns OpenCode exists; no `OpenCodeObservation`/`Finding`/`Result` types. Execution is isolated behind a small purpose-built `IOpenCodeProcessRunner`/`OpenCodeProcessRunner` (NOT a generic shell/command executor): `ProcessStartInfo { UseShellExecute = false }` + `ArgumentList` (`["run", instruction]`) — repo path/prompt are NEVER concatenated into `cmd /c`/`sh -c`; `WorkingDirectory = analyzed repository root` (agent explores; nothing copied/streamed, no M11.3 controlled context); bounded stdout (128 KiB)/stderr (8 KiB); `ExitCode 0` → parse stdout, non-zero → categorized `ProcessError` with bounded diagnostic (no secrets/env/machine details, nothing persisted unlimited). The runner's own `Timeout` (`Evidence:OpenCode:TimeoutSeconds`, default 120) → existing `Timeout` failure category → `Continue`/`FailRun` per `ProviderFailureMode`; a run/user cancellation terminates the process and propagates `OperationCanceledException` (never an ordinary failure, never left running intentionally). Read-only is INSTRUCTION-level only and documented as NOT a security sandbox (OS sandboxing/containers deferred). `ContextFingerprint` stays `""` (success + failure) — never generated from repo hash/snapshot/prompt/working directory, so M12 comparison never treats OpenCode as comparable to Claude/OpenAI API executions. No DeepSeek-specific behavior and no model hardcoding: model identity is unknown (M13.3 configures/validates a specific model explicitly). Config `Evidence:OpenCode { Enabled: false, Executable: "opencode", TimeoutSeconds: 120 }` is disabled by default (offline Mock remains the zero-config default); `--provider OpenCode` opts in via the existing mechanism; no new CLI command. Package stays `schemaVersion **1.1**`, consumer-DTO gate green. | OpenCode is an external runtime: only the infrastructure/provider layer should know it exists; the domain continues seeing `EvidenceProviderType.Agentic` + normal Evidence. Safe argument passing (ArgumentList, no shell string) and WorkingDirectory = repository root avoid quoting/injection problems and keep the agentic model (repo location + task + exploration); instruction-level read-only is honest about not being a sandbox; the shared validator prevents a parallel OpenCode-specific parsing/repair path; timeout vs cancellation must follow the existing M11.1 semantics; the council cannot vouch for the agent's effective context, so no fingerprint and no comparability; model selection is M13.3's job, not M13.2's. | `ADR-022` |
| D-089 | 2026-08-08 | Agentic evidence contract (M13.1): new additive `EvidenceProviderType.Agentic = 6` is the entire abstraction — NO `IAgenticEvidenceProvider` interface (executor/interpreter branch on `Metadata.ProviderType`, never names). An agentic `EvidenceRequest` carries the repository identity + file structure with **all content stripped** plus an explicit **empty** `ContextSelection` (`"agentic"`, zero files; scope still in `TotalRepositoryFiles`) — the council never embeds repository contents. Agentic steps leave `ContextFingerprint = ""` on records AND `Evidence` (success + failure) because the council can't prove the agent's effective context; M12 comparison filters `ProviderType == LLM` only so agentic executions are structurally excluded (no comparison code change). Agentic sources return the same `observations` envelope and `StructuredLlmEvidenceInterpreter.CanInterpret` widens `LLM → LLM or Agentic` with zero interpretation-logic changes (file guard, confidence lowering, discipline-mismatch all apply). Planner untouched (metadata-driven: Agentic+Discipline ⇒ one step per discipline). A real no-network `AgenticEvidenceProvider` (`"Agentic"`, DI-registered) demonstrates the full path end to end; package stays `schemaVersion **1.1**`. | Agentic sources do not fit controlled-context semantics: embedding contents would bundle what the agent ignores/duplicates, and fabricating a fingerprint would claim comparability the system cannot back — honest handling is identity + structure only and fingerprint "unavailable". | `ADR-021` |
| D-088 | 2026-08-08 | Observation calibration diagnostics (M12.4) extends `calibration-diagnostics.json` additively with EXACTLY two new diagnostic types on shared findings (≥2 compared providers) in `Comparable` disciplines only: `ObservationTypeDisagreement` (per-provider distinct normalized `ObservationType` sets differ — exact values, no equivalence rules, never picks a winner) and `LocationDisagreement` (per-provider normalized locations differ — reconciler-style path normalization, exact line when both name one, missing-line compatible, missing location contributes nothing so it never fabricates a conflict; no fuzzy matching, no line-distance tolerance, no `MissingLocation` type). Provider attribution comes ONLY from existing provenance (`Consolidated Finding → SupportingFindingIds → Raw Findings → ObservationIds → EngineeringObservations → SourceProvider | SourceEvidenceId → Evidence.ProviderName`); un-attributable observations are skipped, never guessed from text/titles/ids/ordering. Both signals remain internal: the package is asserted to never carry `observationTypeDisagreement`/`locationDisagreement`, `schemaVersion` stays **1.1**, consumer DTO gate green; every collection sorted so output is independent of enumeration order; M12.3 types/thresholds unchanged. | Diagnostics must stay a pure projection of already-reconciled data — the reconciler's `SupportingFindingIds` is the only authority for which provider findings belong together, so a disagreement within an agreed group can never contradict reconciliation; disagreement is descriptive, not a verdict. | `ADR-020` |
| D-087 | 2026-08-07 | Finding calibration diagnostics (M12.3) is an INTERNAL, deterministic projection producing EXACTLY three diagnostic types — `ExclusiveFinding` (consolidated finding supported by exactly one compared provider; reuses M12.2 `ExclusiveFindingIds` resolved through the consolidated `Findings`), `SeverityDisagreement` (shared finding whose reconciler `SeverityRange` contains an en dash; per-provider severities attributed via `SupportingFindingIds` → a SINGLE compared provider each), `LowAgreement` (per discipline when `ConsolidatedFindingCount ≥ MinimumSampleFindings` (3) and `AgreementRate < LowAgreementThreshold` (0.5); counts copied verbatim from M12.2 `AgreementMetrics`) — produced ONLY for `Comparable` disciplines; `NonComparable`/`Incomplete` become `Limitations` notes only; thresholds are explicit `CalibrationDiagnosticCriteria` constants; `CalibrationDiagnosticsBuilder.Build(run)` returns null for single-provider runs so `calibration-diagnostics.json` is never written; the package is asserted to never carry `calibration`/`exclusiveFinding`/`severityDisagreement` (`schemaVersion` stays **1.1**); every collection sorted, `GeneratedAt=UtcNow`. | Every fact must have one authoritative source already in the run (exclusivity = M12.2 comparison, severity spread = reconciler `SeverityRange`/`SupportingFindingIds`, agreement = M12.2 `AgreementMetrics`) so the artifact can never contradict reconciliation; no semantic matching, no ranking/scoring, no extra provider calls; diagnostics only make sense where providers actually saw the same context. | `ADR-019` |
| D-086 | 2026-08-07 | Provider comparison is an INTERNAL, deterministic projection: `ProviderComparisonBuilder.Build(run)` reads only the run's own records/observations/reconciled findings (no provider calls, no rescan, no context rebuild, no re-interpretation); artifacts `provider-comparison.json`/`.md` are written only when `AnalysisRun.ProviderComparison` is non-null (≥2 LLM providers on a discipline); `run.json` round-trips it additively; the package is asserted to never carry `providerComparison` (consumer DTO `schemaVersion` stays **1.1**). Every collection is sorted so the logical result is independent of enumeration order (only `GeneratedAt` is wall-clock). | A comparison trigger (side-by-side rerun) would be expensive and non-deterministic; internal metrics can evolve freely only if they never leak into the external contract. | `ADR-018` |
| D-085 | 2026-08-07 | Agreement is derived ONLY from the existing `RuleBasedFindingReconciler` output (`SupportingProviders` + `AgreementCount`): shared = consolidated finding supported by ≥2 compared providers, exclusive = exactly one, `AgreementRate = SharedFindingCount ÷ ConsolidatedFindingCount` (null when no consolidated findings), never called "accuracy". SARIF/static-analysis executions are excluded from the comparison, though a deterministic source still corroborates through `SupportingProviders`. | Agreement must come from the one place the system already proves two sources describe the same issue — a new similarity heuristic could silently disagree with reconciliation. | `ADR-018` |
| D-084 | 2026-08-07 | Comparability requires the same discipline AND the same effective context, proven by `ContextFingerprint`; a discipline is `Comparable` (all succeeded, one fingerprint) / `NonComparable` (succeeded, fingerprints differ — execution metrics kept, agreement withheld) / `Incomplete` (an execution failed — output comparison unavailable). Failed/partial disciplines are represented with `Limitations` notes, never dropped and never failing the report. | Two providers could in principle receive different context; only the fingerprint proves equivalence, and partial runs must be represented honestly. | `ADR-018` |
| D-083 | 2026-08-07 | New `ContextFingerprint.Compute(selection, policy?)` = SHA-256 over selected files ordered by relative path with per-file EFFECTIVE content after `ContextContentPolicy.MaxCharactersPerFile` (what the renderer actually produces), `\0`-joined, `"CTX-"` + 12 hex; deliberately excludes absolute paths, provider name, model, API keys, timestamps, RunId. The executor stamps it on every record and `Evidence` (success AND failure paths). | Two providers that received identical effective context must produce the identical value regardless of provider identity or machine/path differences. | `ADR-018` |
| D-082 | 2026-08-07 | Repair/retry/truncation semantics captured per execution: repair max one (valid first ⇒ `RepairAttemptCount=0`, `RepairSucceeded=null`; success ⇒ `1`/`true`; failure ⇒ `1`/`false` + `ErrorCategory="SchemaValidation"`), retries count only transient + timeout retries, and `ResponseTruncated` is surfaced only from provider metadata (`response.Truncated`) with the validator treating truncated output as invalid. | Distinct reliability facts must be observable per step, not conflated into a single success flag. | `ADR-017` |
| D-081 | 2026-08-07 | Token usage is provider-reported only and unknown is never `0`: `InputTokens`/`OutputTokens` come solely from provider usage responses, `TotalTokens = Input + Output` only when both known, and `ProviderExecutionReport`'s `SumKnown` helper sums only known nullable values (null when none known). Run report exposes `TotalInputTokens?`/`TotalOutputTokens?`/`TotalTokens?`/`KnownTokenExecutionCount`. | Unknown usage must not look like a free call; `0` would corrupt downstream sums and any future cost math. | `ADR-017` |
| D-080 | 2026-08-07 | Real provider metrics captured by reusing the existing telemetry surface: per-execution additive fields on records/`Evidence` (`InputTokens?`, `OutputTokens?`, `RetryCount`, `RepairAttemptCount`, `RepairSucceeded?`, `FinishReason?`, `ResponseTruncated`, `ErrorCategory?`), model carried by the existing `ProviderVersion` (stamped from provider metadata, never hardcoded), named run-level aggregates in `ProviderExecutionReport`, and the executor lifting metrics on both success and throw paths (`LlmProviderException` extended additively; run-token timeout ⇒ `ErrorCategory="Timeout"`). `engineering-review-package.json` unchanged (`schemaVersion` stays **1.1**); `EngineeringMetrics` untouched; `provider-execution.json` remains the single detailed artifact. | Facts only — no provider comparison/calibration/cost optimization; real executions should populate existing telemetry without a contract change or new architecture. | `ADR-017` |
| D-079 | 2026-08-07 | Context "considered" vs "selected" clarified (C6): `ProviderExecutionRecord` gains additive `ContextFilesConsidered`, stamped by the executor from `selection.TotalRepositoryFiles` (the scope the selector ranks; constant across steps). `AcquisitionCoverage.ContextFilesConsidered` now reports that repository scope instead of `Max(r.ContextFileCount)`; `ContextFilesSelected` stays the sum of per-step selections. | The per-step max was a misleading run-level "considered" figure — it conflated selected with considered and undercounted the evaluated scope. | `ADR-016` |
| D-078 | 2026-08-07 | One authoritative context content rule (C4): new `ContextContentPolicy` (`Core/Analysis`) defines `EffectiveContextCharacters(file) = min(actualLength, perFileLimit)`, shared by `RuleBasedAnalysisContextSelector` (character budgeting) and `RepositoryContextBuilder` (per-file truncation). New `Evidence:Context:MaximumCharactersPerFile` (default 8000) wired through `EvidenceOptions`/`CouncilOptions`, parsed by CLI + API, applied via one DI-registered policy shared by both. | Selection budgeting used full file length while the renderer truncated at a separate 8000-char constant — `EstimatedContentSize` overstated the rendered context and the two could disagree. | `ADR-016` |
| D-077 | 2026-08-07 | Shared analyzer-registration validation (fail fast): new `AnalyzerDisciplineValidator` (`Core.Application`) + `UnsupportedDisciplineException` used by CLI, API, and the `AnalysisPipeline` entry. The pipeline validates the effective discipline set (`request ?? config`) BEFORE the scan and refuses zero registered analyzers; the CLI exits 1 and the API returns 400, both reporting ALL unsupported disciplines. | A valid-enum discipline with no registered analyzer was silently dropped and produced an Excellent "no findings" run; every entry point must share one check. | `ADR-015` |
| D-076 | 2026-08-07 | Unknown discipline is never CodeQuality: `StructuredLlmEvidenceInterpreter` maps an unrecognized discipline string to `FindingCategory.Unknown`; `EngineeringObservation.Discipline` and `Finding.Category` defaults change to `Unknown`. No Unknown analyzer; unknown observations stay persisted, provenance-preserved, and unconsumed. New additive `UnclaimedObservationCount` → `EvidenceSummary.UnclaimedObservations` + `EngineeringMetrics.UnclaimedObservations`. | Unknown evidence was silently re-labelled CodeQuality and inflated CodeQuality metrics; surfacing it unclaimed is honest. | `ADR-015` |
| D-075 | 2026-08-07 | Provider metrics are mutually exclusive: `EngineeringMetrics` classifies each provider with ≥1 execution as exactly one of Successful (all succeeded) / Partial (`PartialProviders`, ≥1 success AND ≥1 failure) / Failed (all failed) by execution outcome — never evidence count; zero executions is never counted. `SuccessfulProviders`/`FailedProviders` retained for compatibility; `PartialProviders` additive. | The old predicates overlapped (mixed provider counted Successful, no-evidence provider counted Failed) and hid partial failures. | `ADR-015` |
| D-074 | 2026-08-07 | runId path traversal defense in depth: single source of truth `AnalysisRunId` (generation + strict format `^[0-9]{8}-[0-9]{6}-[0-9a-f]{6}$`), full-path resolution verified under `OutputsRoot`, `GetAsync` returns not-found for invalid/escaping ids, `SaveAsync` rejects them, `GET /runs/{runId}` returns 400 (malformed) / 404 (unknown). No paths/contents in error bodies. | Run ids are output-folder names; untrusted ids must never escape the outputs root. | `ADR-014` |
| D-073 | 2026-08-07 | Cancellation stops the pipeline: `AnalysisPipeline.RunAsync` catches `OperationCanceledException` (run token cancelled) before the generic failure handler, marks the run `AnalysisRunStatus.Cancelled` (new additive enum value), and rethrows — no reconciliation, no package build, no persistence. API `POST /runs` returns `499 Client Closed Request`, never HTTP 200. | Cancellation is not an ordinary failure; persisting a package for a cancelled run misleads consumers. | `ADR-014` |
| D-072 | 2026-08-07 | LLM timeout vs cancellation by token ownership, not message inspection: the transport only ever sees the provider's linked timeout token, so in `LlmEvidenceProvider.SendWithRetryAsync` a `Cancelled`-reported failure is a provider TIMEOUT unless the run token is genuinely cancelled — timeouts retried per `MaxRetries` and surfaced as `LlmErrorCategory.Timeout`; run cancellation never retried. Telemetry logs the two distinctly. | Previously real-provider timeouts were misclassified as `Cancelled` (non-transient) and never retried despite configuration. | `ADR-014` |
| D-071 | 2026-07-24 | Evaluation dataset = 5 small, deterministic, self-authored fixtures with documented `evaluation.json` purposes (clean control, vulnerable+SARIF, tangled architecture, fragile reliability, undocumented/untested). Fixture defect code is scanned as text, never compiled/executed. | Reproducible, shareable calibration; a false-positive control is as important as defect cases. | `ADR-013` |
| D-070 | 2026-07-24 | Cost estimation is optional configuration (`Evaluation:Pricing`), never domain logic. Absent pricing or absent provider-reported usage ⇒ cost reported as unavailable, never invented; the external consumer never depends on cost fields. | Pricing changes and varies by provider/model; guessing would mislead. | `ADR-013` |
| D-069 | 2026-07-24 | `EvaluationReportExporter` writes an INTERNAL `evaluation-report.md` presenting measurements only — it never declares a winner, ranks providers, or assigns weights (guarded by a test). Provider comparison is over identical inputs. | Calibration must inform, not prejudge; ranking/weighting are explicit non-goals. | `ADR-013` |
| D-068 | 2026-07-24 | Milestone 012 adds a measurement layer (additive diagnostic `AnalysisRun.StageTimings` + `ObservationInterpretationSummary.InvalidFileReferencesDropped`, pure `EvaluationMetricsCollector`, `EvaluationRunner`, `evaluate` CLI verb) around the existing pipeline. No analysis capability added; `engineering-review-package.json` stays `schemaVersion 1.1` and the consumer-DTO gate is unchanged. | Calibrate before councils: build the baseline/harness before any non-deterministic addition. | `ADR-013` |
| D-067 | 2026-07-23 | Opt-in real integration tests (`RUN_CLAUDE_INTEGRATION_TESTS` / `RUN_OPENAI_INTEGRATION_TESTS` + key), tiny fixture, one discipline, structural assertions only. Normal suite runs on scripted fake clients — no internet, credentials, or paid calls. | CI must stay free, deterministic and offline; live wording can never be asserted. | `ADR-012` |
| D-066 | 2026-07-23 | External providers disabled by default; explicit selection opts in. An explicitly selected provider with no key is NOT silently swapped for Mock (supersedes the ADR-002 fallback for explicit selection) — it reports "Claude requires the environment variable ANTHROPIC_API_KEY." `Evidence:ProviderFailureMode` = `Continue` (default, isolate) or `FailRun`. Unconfigured runs still default to offline Mock. | Sending source code externally must be deliberate; silent fallback hides misconfiguration. | `ADR-012` |
| D-065 | 2026-07-23 | LLM responses are validated BEFORE interpretation: conservative extraction (direct JSON or exactly one fenced block; multiple documents/unbalanced/prose-heavy rejected), schema version + discipline match + observation shape + truncation checks, and at most ONE repair attempt. Retries only for transient categories (rate limit/timeout/network/5xx). | Prose must never be silently converted into evidence; repair loops and retry storms are prevented. | `ADR-012` |
| D-064 | 2026-07-23 | One shared prompt (`IEvidencePromptBuilder`) and one versioned provider-neutral response schema (`schemaVersion`+`discipline`+`observations`) for every LLM source, compatible with the existing `StructuredLlmEvidenceInterpreter`. Transport isolated behind `IClaudeClient`/`IOpenAiClient`. | Two schemas would leak provider knowledge downstream; client seams make the suite offline-testable. | `ADR-012` |
| D-063 | 2026-07-23 | Real `ClaudeEvidenceProvider` + `OpenAiEvidenceProvider` as discipline-scoped `IEvidenceProvider` adapters sharing one base; no provider-name branching anywhere. The `Codex` stub is retired — a code-focused model is just `Evidence:OpenAI:Model`, with `Codex` kept as a config alias. Package stays `schemaVersion 1.1` (no consumer-visible change). | Validate the architecture with real model evidence without touching analyzers, reconciliation, or the external contract. | `ADR-012` |
| D-062 | 2026-07-22 | Integration contract gated by a consumer-facing DTO (`PackageContract`) + test that deserializes `engineering-review-package.json`; serialization asserts stable camelCase names, string enums, and no leaked .NET types. Preserved `artifacts/samples/engineering-review-package.sample.json`. | The external app must ingest the package without reading diagnostic artifacts; a contract test prevents silent breakage. | `ADR-011` |
| D-061 | 2026-07-22 | Package additive `schemaVersion "1.1"` (legacy `version "1.0"` kept) + provider-neutral `reconciliation` summary + `Appendix.ReconciliationGroups`. Additive→minor, breaking→major. | Explicit versioning protects older consumers; single official integration artifact preserved. | `ADR-011` |
| D-060 | 2026-07-22 | Deterministic `IFindingReconciler`/`RuleBasedFindingReconciler` runs BEFORE the package builder: staged grouping (rule+location / type+symbol / title+location / separate), provider-independent `AgreementCount`, highest-severity + range, capped confidence boosts (never averaged, no weights), contradiction flags, stable content-hashed ids. Prefers false negatives to bad merges. Pipeline consolidates via it; `IFindingMerger` retained for backward compat. | One consolidated, traceable, reproducible finding set from many sources without an LLM. | `ADR-011` |
| D-059 | 2026-07-22 | `Finding` extended additively (SupportingFindingIds/AgreementCount/Severity+ConfidenceRange/ReconciliationReason+Strategy/IsConsolidated/HasContradiction/ContradictionReasons/Symbol+LineReferences/Metadata); `AnalysisRun` +ReconciliationSummary/Groups. No renames. | Expose reconciliation provenance without breaking the stable Finding contract. | `ADR-011` |
| D-058 | 2026-07-21 | Package gains `StaticAnalysisSources` (Tool/Version/ImportedResults/GeneratedObservations), derived from `AnalysisRun.Evidence` + observations; rendered as a concise `## Static Analysis Sources` markdown appendix (never raw SARIF). `AnalysisRun` now carries raw `Evidence`. | Executive-friendly metric proving what a deterministic source imported vs produced; provider-agnostic (no SARIF types in the central model). | `ADR-010` |
| D-057 | 2026-07-21 | `SarifEvidenceInterpreter` owns all SARIF semantics: deterministic severity (error→Critical/warning→High/note→Medium/none→Low), discipline (keyword scan of id+name+desc+tags, else Unknown), type (else Unknown), confidence (0.95 default, reduced per concrete deficiency). Added `FindingCategory.Unknown`, `ObservationTypes.Unknown`, `EngineeringObservation.ColumnReferences`. | Only the interpreter understands the schema; analyzers stay provider-independent; meaning is never invented. | `ADR-010` |
| D-056 | 2026-07-21 | `IEvidenceProvider.CollectAsync` returns `IReadOnlyList<Evidence>` (was single). Executor stamps provenance on each item, records `EvidenceCount=n` for the one step. | A repository-scoped SARIF import yields one Evidence per run without weakening the provider abstraction or the planner/executor. | `ADR-010` |
| D-055 | 2026-07-21 | Native `SarifEvidenceProvider` (Static, Repository-scoped) imports existing SARIF 2.1.0 files (no scanner execution), one Evidence per run, preserving tool/version/invocation/counts/original payload. Enabled via `Evidence:Sarif` config or CLI `--sarif` (repeatable); auto-included when active. | First real deterministic evidence source; validates heterogeneous sources without touching analyzers. | `ADR-010` |
| D-054 | 2026-07-17 | Discipline-mismatch rule: a discipline-scoped observation whose discipline ≠ the requested discipline is not rewritten — confidence is lowered, it is tagged `discipline-mismatch`, and the count surfaces in the interpretation summary. `Evidence`/`EngineeringObservation` gain `AcquisitionStepId`; `AcquisitionCoverage` added to the package. | Honest handling of provider drift; full step-level traceability; provider-agnostic coverage in the deliverable. | `ADR-009` |
| D-053 | 2026-07-17 | Planner records unsupported provider/discipline combinations in `Plan.UnsupportedCombinations` (no step created); `EvidenceProviderMetadata` carries `Name`/`ProviderType`/`Version` and is the provider's only identity. Config normalizes `["Mock"]` or `[{Name,Scope}]`, `Evidence:Context:{MaximumFiles,MaximumCharacters}`; CLI/API validate disciplines before analysis. Added SARIF/Git/Coverage repository-scoped stubs. | Full contract compliance; deterministic, inspectable topology; fail-fast on bad input. | `ADR-009` |
| D-052 | 2026-07-17 | Rich `AnalysisContextSelection` (strategy/files/totals/estimated size/reasons) replaces the thin `ContextSelection`; selector honors file/char limits by including whole files (never truncating) and records exclusions. | Deterministic, budget-aware context with explainable selection. | `ADR-009` |
| D-051 | 2026-07-17 | Executor **owns** telemetry: `ExecuteAsync(plan, repo, ct)` returns `EvidenceAcquisitionResult { Evidence, Executions, Plan }`. The `IProviderExecutionCollector` (D-050) is removed entirely — no process-global state. | Return-value telemetry is simpler and structurally guarantees run isolation. | `ADR-009` (supersedes D-050) |
| D-050 | 2026-07-16 | Run-scoped `IProviderExecutionCollector` (created per run) replaces the process-global resettable `IProviderExecutionLog`. *(Superseded by D-051: executor now owns/returns records.)* | Removes the concurrency limitation before parallelism; telemetry never interleaves across runs. | `ADR-009` |
| D-049 | 2026-07-16 | Explicit `EvidenceAcquisitionPlan` (planner) drives topology; the pipeline executes the plan instead of implicit loops. Deterministic; written into provider-execution/run/package. | Inspectable, testable, single place for topology decisions. | `ADR-009` |
| D-048 | 2026-07-16 | `IAnalysisContextSelector` selects discipline-relevant files (rule-based, no embeddings); focused prompts restored via `DisciplinePrompts`. | Restore per-discipline depth lost by one repository-wide prompt. | `ADR-009` |
| D-047 | 2026-07-16 | Planner branches only on `EvidenceProviderMetadata` (scope/disciplines) + config overrides — never on concrete provider names. | Extensible topology; no name switch. | `ADR-009` |
| D-046 | 2026-07-16 | `EvidenceRequest` first-class (RunId/Scope/Discipline/Instructions/ContextSelection/CorrelationId); `IEvidenceProvider.GatherEvidenceAsync`→`CollectAsync`, add `Metadata`. Observation+Evidence carry acquisition provenance. | Discipline-aware acquisition + Finding→…→provider execution traceability. | `ADR-009` |
| D-045 | 2026-07-16 | Two acquisition scopes: Repository (static sources, once) + Discipline (LLM, once per discipline). Config `Evidence:Providers` accepts `{Name,Scope}`; CLI `--disciplines`. | Preserve specialized depth while supporting repository-wide sources (Sonar/SARIF/Roslyn/Git/coverage). | `ADR-009` |
| D-044 | 2026-07-16 | Future interpreters (SARIF/Sonar/Roslyn/Semgrep/NDepend/Git/Coverage) are **unregistered, non-throwing skeletons** (`CanInterpret`→false), not `NotImplementedException`. | Establish shape without polluting discovery or risking runtime throws. | `ADR-008` |
| D-043 | 2026-07-16 | Evidence acquired **once centrally** by the pipeline (general observation-collection prompt), not per-analyzer; analyzers consume observations only. | Separates acquisition/interpretation/analysis; one LLM call set per run; enables non-LLM sources. | `ADR-008` |
| D-042 | 2026-07-16 | `FindingsNormalizer` removed; JSON parsing + "do not invent files" guard moved into `StructuredLlmEvidenceInterpreter`; `ExtractJson`→`Core.Serialization.JsonExtraction`. | Parsing is an interpretation concern, not an analyzer one. | `ADR-008` |
| D-041 | 2026-07-16 | `Finding` gains observation provenance (`ObservationIds`, `SourceRules`, `SupportingProviders`, `SupportingObservationCount`); traceable Finding→Observation→Evidence→provider. | Explainability across heterogeneous sources. | `ADR-008` |
| D-040 | 2026-07-16 | Interpreter resolution by **discovery** (`CanInterpret`), never a provider-name switch; unmatched evidence recorded as *unsupported* (never invented). | Extensible without central switch; honest telemetry. | `ADR-008` |
| D-039 | 2026-07-16 | Introduce normalized `EngineeringObservation` + Evidence Interpreter layer (`IEvidenceInterpreter`/resolver/pipeline). Analyzers consume observations. | Stable, source-neutral language between heterogeneous evidence and analyzers. | `ADR-008` |
| D-038 | 2026-07-07 | `run-summary.md` / `council-summary.md` subsumed by `engineering-review.md`; artifact set is package-centric (`engineering-review-package.json` + `engineering-review.md` primary). | The package is the deliverable; reduce artifact sprawl; Markdown is one projection. | `ADR-007` |
| D-037 | 2026-07-07 | `EngineeringHealth`/`EngineeringRisk` computed by a documented rule-based `HealthRiskScorer` (thresholds in ADR-007). No LLM. | Explainable, reproducible, testable; swappable later behind the same fields. | `ADR-007` |
| D-036 | 2026-07-07 | Exporters project the package: `MarkdownReportGenerator` → `EngineeringReviewMarkdownExporter`; `JsonReportGenerator.RenderPackage`. Builder has no exporter logic. | One source of truth; presentation evolves without breaking consumers. | `ADR-007` |
| D-035 | 2026-07-07 | Git branch/commit captured read-only via `GitProbe` (no git exe); graceful "(unknown)" when not a repo. | Package needs repo identity without a hard git dependency. | `MILESTONE-006` |
| D-034 | 2026-07-07 | Introduce `EngineeringReviewPackage` as the first-class deliverable; `IEngineeringReviewPackageBuilder`; CLI `review` verb; repository persists the package. | Everything the platform produces enriches one versioned assessment object. | `ADR-007` |
| D-033 | 2026-07-07 | Rule-based merger does **not** merge findings across different `EvidenceProvider`s. | Preserve per-provider attribution; cross-provider reconciliation/voting is a future milestone. | `ADR-006` |
| D-032 | 2026-07-07 | Provider execution telemetry captured via a run-scoped `IProviderExecutionLog` singleton (reset by the pipeline); written to `provider-execution.json`. | Cross-analyzer aggregation without changing the `IAnalyzerAgent` return type. Concurrent runs deferred with parallel execution. | `MILESTONE-005` |
| D-031 | 2026-07-07 | Config `Evidence:DefaultProvider` (single) → `Evidence:Providers` (list); CLI adds `--providers a,b`. `SelectedProvider` → `SelectedProviders`. | Multiple providers per analysis. | `ADR-006` |
| D-030 | 2026-07-07 | Analyzers depend on `IEvidenceExecutor` (not the factory); the executor runs ALL configured providers and returns `IReadOnlyList<Evidence>`; the analyzer interprets each. | Multiplicity without coupling; failure isolation; per-provider attribution. | `ADR-006` |
| D-029 | 2026-07-07 | `EvidenceExecutor` is sequential but isolates per-provider work in one method; failures/timeouts become failed `Evidence` and never stop other providers. | One-method switch to `Task.WhenAll` later; robustness now. | `ADR-006` |
| D-028 | 2026-07-07 | Extend `Evidence` (`ProviderId`, `ProviderVersion`, `Success`, `ErrorMessage`, `Duration`, `CostEstimate`) and `Finding.EvidenceProvider`. | Model per-call outcome + provider provenance for the future council. | `ADR-006` |
| D-027 | 2026-07-06 | LLM evidence provider named `ClaudeEvidenceProvider`; underlying chat client is OpenAI-compatible (`OPENAI_*`) as a stand-in, swappable in DI. | Names the intended Claude Code backend without blocking on a real client; keeps zero-setup behavior. | `ADR-005` |
| D-026 | 2026-07-06 | Future providers (`Codex`, `Ollama`, `Roslyn`, `Sonar`, `Semgrep`, `NDepend`) are registered `NotImplementedException` stubs. | Establish the architecture/shape now; fill bodies in a later milestone without touching analyzers. | `ADR-005` |
| D-025 | 2026-07-06 | Effective-default resolution: requested provider falls back to `Mock` when the LLM provider has no API key. | Preserve zero-setup runs; `--provider`/`Evidence:DefaultProvider` still honored when available. | `MILESTONE-004` |
| D-024 | 2026-07-06 | Microsoft Agent Framework integration moved from the analyzer base into `ClaudeEvidenceProvider`; `EngineeringCouncil.Agent` drops the `Microsoft.Agents.AI` dependency. | Analyzers must not know their evidence backend or its SDK. | `ADR-005` |
| D-023 | 2026-07-06 | Analyzers depend on `IEvidenceProviderFactory` (+ `EvidenceOptions`), not `ILLMProvider`; base renamed `ChatAnalyzerAgent` → `EvidenceBasedAnalyzer`. Providers return `Evidence`, never `Finding`s. | Reason over evidence, not raw LLM responses; decouple analyzers from providers. | `ADR-005` |
| D-022 | 2026-07-06 | Introduce Evidence Provider layer: `IEvidenceProvider`, `IEvidenceProviderFactory`, `Evidence`, `EvidenceProviderType`. Remove `ILLMProvider`/`OpenAILLMProvider`/`MockLLMProvider`/`LlmProviderChatClient`. | Prepare for multi-provider/multi-agent council over heterogeneous sources (LLMs + static analyzers + tools). | `ADR-005` |
| D-021 | 2026-07-06 | `Duplicate`/`Discarded` statuses + `DuplicateOf` exist in the model but are not emitted by the rule-based merger yet. | Reserved for the future LLM council; keeps the contract stable. | `ADR-004` |
| D-020 | 2026-07-06 | Council summary + merger are **rule-based** (no LLM) in v3. | Deterministic, testable, offline; the seam is ready for an LLM reconciler later. | `ADR-004` |
| D-019 | 2026-07-06 | Orchestrator assigns global raw ids (`RAW-NNN`) at aggregation. | Analyzers number findings locally (F-001…) and collide once aggregated; merger needs unique ids for provenance. | `MILESTONE-003` |
| D-018 | 2026-07-06 | `findings.json` now = **consolidated** findings; raw analyzer output preserved separately in `raw-findings.json`. | Output must no longer be a raw concatenation; keep an audit trail. | `ADR-004` |
| D-017 | 2026-07-06 | Add consolidation layer: `IFindingMerger`/`RuleBasedFindingMerger` + `ICouncilSummaryGenerator`/`RuleBasedCouncilSummaryGenerator`; pipeline = scan→analyze→raw→merge→summary→persist. | First council-like behavior: dedup, severity/confidence reconciliation, executive summary. | `ADR-004` |
| D-016 | 2026-07-06 | Extend `Finding` with `Status`, `DuplicateOf`, `MergedFromFindingIds`, `SourceAgents`, `Severity/ConfidenceRationale`; keep `SourceAgent` for backward compat. | Model the consolidation lifecycle and multi-agent attribution. | `ADR-004` |
| D-015 | 2026-07-06 | `LlmProviderChatClient` folds `ChatOptions.Instructions` into the system prompt. | Agent Framework passes agent instructions via `Instructions`, not a system message; without this the specialty prompts were dropped. | `MILESTONE-002` |
| D-014 | 2026-07-06 | `Core` now references **only** `Microsoft.Extensions.Logging.Abstractions` (was zero deps). | Orchestrator (Application layer) needs to log analyzer execution/failures; abstractions-only keeps the domain clean. | — |
| D-013 | 2026-07-06 | Reporting moved to dedicated exporters: `IMarkdownReportGenerator` + new `IJsonReportGenerator`. Repository uses both. | Orchestrator/pipeline must not know report formats; formats evolve independently. | `MILESTONE-002` |
| D-012 | 2026-07-06 | New `AnalysisPipeline` owns scan→orchestrate→persist; `AnalysisOrchestrator` is now analyzer-coordination only (`IEnumerable<IAnalyzerAgent>` → `List<Finding>`). | Separates the end-to-end flow from analyzer coordination; keeps CLI/API behavior. | `ADR-003` |
| D-011 | 2026-07-06 | Analyzers register as `IEnumerable<IAnalyzerAgent>` (`AddAnalyzer<T>()`); orchestrator auto-discovers. | Adding a discipline = implement + register only, no orchestrator change. | `ADR-003` |
| D-010 | 2026-07-06 | `IAnalyzerAgent` = `{ Name, Category, AnalyzeAsync }`, returns only `IReadOnlyList<Finding>`. Analyzers never render/write/know-dashboard. | Clean, single-responsibility analyzers. | `ADR-003` |
| D-009 | 2026-07-06 | Replace the single generic `AnalyzerAgent` with 7 specialized analyzers on a shared `ChatAnalyzerAgent` base + focused per-discipline prompts. | Focused agents produce deeper, better-attributed, reliably-covered findings; foundation for the Council. | `ADR-003` |
| D-008 | 2026-07-06 | Central Package Management via `Directory.Packages.props`; shared TFM/nullable via `Directory.Build.props`. | One place to pin versions across 7 projects. | — |
| D-007 | 2026-07-06 | Guardrail: normalizer drops `fileReferences` not present in the snapshot. | Enforces "do not invent files" in code, not just the prompt. | `prompts/findings-normalizer.md` |
| D-006 | 2026-07-06 | Output artifacts: `findings.md`, `findings.json`, `run-summary.md`, `run.json` under `outputs/{runId}/`. | Human + machine readable; dashboard consumes `findings.json`; `run.json` enables reload. | — |
| D-005 | 2026-07-06 | Analyzer runs on **Microsoft Agent Framework** via `IChatClient.AsAIAgent(...)`; `ILLMProvider` is a text-based backend seam bridged by `LlmProviderChatClient`. | Agent Framework = runtime; provider seam = pluggable heterogeneous backends. | `ADR-002` |
| D-004 | 2026-07-06 | Provider auto-selection with **mock fallback** when `OPENAI_API_KEY` is absent. | Pipeline runs offline with zero setup. | `ADR-002` |
| D-003 | 2026-07-06 | Read-only scanner ignoring `bin, obj, .git, node_modules, packages, artifacts, .vs`; content-load caps. | Safe, cheap, never mutates the target. | — |
| D-002 | 2026-07-06 | Fresh domain (`AnalysisRun`, `Finding`, enums) + `EngineeringCouncil.*` namespaces; no FM names. | Required isolation; domain free to evolve. | `ADR-001` |
| D-001 | 2026-07-06 | Reuse FM's **workflow pattern only** (memory, decision log, ADRs, milestones, prompts, outputs) — not its code or domain. | Familiar operating rhythm, zero blast radius. | `ADR-001` |

## Notes / gotchas

- **Agent Framework API (1.13.0):** the extension is `AsAIAgent` (not
  `CreateAIAgent`) in namespace `Microsoft.Extensions.AI`; `RunAsync` returns
  `AgentResponse` (not `AgentRunResponse`). Discovered during first build.
- .NET 10 `dotnet new sln` produces `.slnx` (XML) by default.
